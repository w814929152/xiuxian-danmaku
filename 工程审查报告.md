# 工程审查报告 · 弹幕修仙

审查人：Godot Gameplay Scripter（节点通）
引擎：Godot 4.7.2.stable · 验证：headless 导入 + **110 项**自动化断言（每步改动后复跑）

## 总评（Phase 9 完成后）

| 维度 | 结论 |
| --- | --- |
| 可运行性 | ✅ 导入 0 错误、主场景 0 错误、自测 **134/134 PASS** |
| 静态类型 | ✅ 全量类型化；6 项严格 GDScript 警告已开启且 **0 warning** |
| 信号完整性 | ✅ 全 `snake_case` + 参数带类型；关卡与 UI 之间改走信号 |
| 节点组合 | ✅ 0 处 `get_parent()`（24 处已改为显式 `world` 注入） |
| Autoload 卫生 | ✅ 绘制工具在 `DrawUtil`，`Game` 只剩全局状态（+ 退出清池） |
| 运行时告警 | ✅ 0 ERROR / 0 WARNING / 0 RID 泄漏 |
| 每帧开销 | ✅ 绘制指令 ~370 → ~105，三角函数 ~1300 → ~120 次/帧 |

当前状态：**能跑、结构干净、类型完备、性能余量充足**。

## 已完成的重构

### Phase 9 · 护法妖将（精英怪，3 文件）

需求：加一只精英怪。用户拍板：**护法妖将 · 属性法罡**，第 2、3 重妖潮压轴各一只。

- 新增 `entities/Elite.gd`：身披属性法罡 —— 法罡存续时异色飞剑只剩 35%、**本体不掉血**；
  破罡进虚弱（4.5 秒，移速减半 / 出手放慢 / 任意颜色全额）；虚弱结束重铸，
  共 2 层，用完永久破防。斩杀 +800 且必掉一件道具。

**9a 法罡色必须从玩家道袍里抽 —— 这是可行性要求，不是机制复刻**

老祖法罩已经这么做了。妖将沿用，但理由更硬：法罡不破本体不掉血，
若凭空抽一个玩家没带的颜色，玩家就只能以 35% 的伤害刮 200 多点罡气 ——
那不是难度，是死局。`setup()` 里 `player_robes` 为空才退回随机四色（自测直连时用）。

**9b 「一击打爆法罡」的用例陷阱**

同色共振 ×1.5 意味着 100 点伤害打出来是 150 —— 恰好一击打爆 150 的法罡，
第一版用例因此连破两层（`layers` 断言 FAIL）。改用 20 点伤害验证比例（20 → 30），
把「衰减比例」和「恰好破罡」两个命题分开测。

**9c 破罡提示的可见性靠实机取帧校验**

法罡破了之后裂环画得太淡（alpha 0.32），取帧图上几乎看不出状态变化 ——
状态机在代码里是对的，玩家看不见就等于不存在。提到 0.5 并加了亮度。
`_capture/CaptureElite.gd` 可一键拍三态（法罡完整 / 过半 / 破罡）。

**9d 波次清场把妖将当硬性门槛**

小妖剩最后一只不等（保持原节奏），妖将没斩就**不许**进下一重
（`_enemy_count() <= 1 and not _elite_alive()`）；清场上限在精英波放宽到 26 秒 ——
斩一只妖将约 8~12 秒，14 秒的窗口会把它卡在半路。
`_stop_field()` 同步补了妖将清理（玩家陨落时撤场）。

**9e 飘字挂 world 而不是挂自己**

Enemy 的 `hit()` 飘字挂在自身（`Fx.pop(self, ...)`），若敌人在飘字存活期内死亡，
池化 Fx 会跟着 `queue_free`，而池里还留着它的引用 —— 下次取出来就是已释放对象。
既有隐患，妖将（活得久、飘字多）必然踩到，所以 Elite 统一挂 `world`。
Enemy 那边没改（存活期与飘字期重叠概率低，留给后续清理）。

### Phase 9 补 · 狂暴屏幕血光（HUD）

需求：狂暴后屏幕周围闪红。落在 `HUD._rage_vignette()`，三个节奏叠在一起：

- **呼吸**（约 3 秒周期）：血光缓慢起伏，不是一层死贴纸；
- **心跳**（1.8 秒周期 / 0.2 秒尖峰）：尖峰平方衰减 ——「闪」靠的是这个突变，不是渐变；
- **强闪**（0.75 秒）：刚跌破三成那一下连画面中心都带上一点血色。
  狂暴是局面转折点，玩家需要一帧就能读到的信号，不能只靠横幅。

三条实现要点：

1. **放在 HUD 而不是 Boss / Level**：全屏氛围属于 UI 层；HUD 已经持有 `boss` 引用
   （沿用 `is_instance_valid` 卫生检查），狂暴的**边沿检测**也顺手在 HUD 侧做
   （`_was_enraged` 前后帧对比）—— 不为这一个表现给 Boss 加信号、让 Level 转发。
2. **多层矩形而不是渐变填充**：CanvasItem 没有渐变 API，`draw_primitive` 的顶点色
   插值依赖渲染驱动。20 层 × 9px = 180px 覆盖深度，透明度按平方向外递增，
   实机取帧上看不出台阶。
3. **零常驻开销**：只在 `boss.enraged` 时才画（约 80 条指令 / 帧），
   普通战斗与狂暴前一帧指令都不多花。

验证：`_capture/CaptureRage.tscn` 把老祖血量压到 22% 触发狂暴连拍四帧 ——
强闪帧四周浓红 + 中心染色，常态帧只剩边缘红晕，节奏差异一眼可辨。
（取帧脚本里给玩家挂了 `invinc = 999`，画面左上角的「无量」小牌是脚本痕迹，
正常游戏不会出现。）


## 已完成的重构

### Phase 1 · 真 bug 修复（3 文件）

| 文件 | 问题 | 修法 |
| --- | --- | --- |
| `Sword.gd` | 一帧内命中多个目标**重复 `queue_free()`** | 重入守卫 |
| `Danmaku.gd` | 清屏 `dissolve()` 与本帧超时释放叠加 | 同样加守卫 |
| `Background.gd` | 74 顶点凹多边形每帧三角化失败（1001 次报错） | 拆成 72 个凸四边形 |

### Phase 2 · 解耦（7 文件）

**2a 消除 `get_parent()`**：`Player` / `Enemy` / `Boss` / `Sword` / `Danmaku` 各加 `var world: Node2D = null`，
由 `Level` 注入；四个生成点加 null 保护并把 `world` 透传给子弹。共 24 处。

**2b HUD 不再反向依赖 Level**：HUD 改为持有 `player` / `boss` / `score` / `wave_text`，
通过 `bind()` / `bind_boss()` / `set_score()` / `set_wave()` 接收数据，
操作通过 `pause_toggled` / `restart_requested` 信号上报。

### Phase 3 · 抽象与类型化

**3a `Damageable` 抽象（4 文件）**

新增 `scripts/entities/Damageable.gd`（`extends Area2D`，含 `hit(dmg, c)`，未重写会 `push_warning`）。
`Enemy` 与 `Boss` 改为继承它；`Sword._on_area_entered` 从两条 `is Enemy / is Boss` 分支
简化为一次 `if a is Damageable: (a as Damageable).hit(damage, color)`。
以后加第三种可击杀目标，Sword 一行都不用改。

**3b 关卡与 UI 之间改走信号（4 文件）**

- `Level` 新增 `signal finished(win: bool)` 与 `signal restart_requested()`
- 删掉 `_finish()` 和 `_on_restart_requested()` 里的 `get_node_or_null("/root/Main")` + `has_method` 硬查找
- `Main.start_level()` 连接：`s.finished.connect(show_result)`、`s.restart_requested.connect(restart_level)`
- `Main` 加 `class_name Main`，便于类型化引用
- `Boss` 的 `POOL` / `DUR` / `TICK` 类型化为 `Dictionary[int, Array]` 与 `Dictionary[String, float]`

> 踩坑：`PackedStringArray([...])` 在 `const` 里不算常量表达式，会报
> `Assigned value for constant "POOL" isn't a constant expression` —— 常量字典的值用数组字面量即可。

**3c 拆分绘制工具（6 文件）**

新增 `scripts/DrawUtil.gd`（`class_name DrawUtil extends RefCounted`，纯静态）：
`font()` / `tw()` / `txt()` 全部迁入，自带字体缓存。
`Game.gd` 删除字体与绘制相关成员，只留四色常量、输入映射、跨场景数据。
调用点批量改为 `DrawUtil.txt(...)` / `DrawUtil.tw(...)`：`Fx` / `HUD` / `SkinSelect` / `TitleScreen` / `ResultScreen`。

### Phase 4 · 性能与稳定性（6 文件）

**4a 背景绘制烘焙（`Background.gd`）**

| 元素 | 改前 | 改后 |
| --- | --- | --- |
| 夜空渐变 | 26 条 `draw_rect` 色带 | 1 张烘焙好的 `GradientTexture2D` |
| 远山三层 | 3 × 72 = **216** 个 `draw_colored_polygon` | 3 张周期纹理，**6** 次 `draw_texture_rect_region` |
| 星野 | 110 个 `draw_circle` | 1 张周期纹理（2~3 段）+ 26 颗保留逐个明灭的亮星 |
| 云海 | 每帧重算 16 × 28 个椭圆顶点 | 单位椭圆顶点预计算，逐帧只做 `Transform2D` 变换 |

要点：

- 山脊改用**共享周期的三个谐波**（`1 : 3 : 7`，周期 `PERIOD = 1600`），保证纹理可无缝平铺；
  纹理按 `TEX_SCALE = 2` 水平压缩（800 × ~370），线性过滤后边缘自然柔化。
- 纹理在 `_ready()` 里用 `Image.fill_rect` 逐列烘焙，三层合计约 2400 次调用（毫秒级，只在启动时一次）。
- 逐帧只做 `_tile()`：按视差把偏移折算成纹理列，`fposmod` 取模后最多 2~3 段铺满屏幕宽。
- 每帧三角函数：~1300 → ~120 次。

**4b 对象池（新增 `scripts/Pool.gd`）**

`class_name Pool extends RefCounted`，静态字典分桶，容量上限 512：

- `Pool.acquire(key, parent, factory)` —— 池空才 `factory.call()` 新建，否则摘出来复用
- `Pool.release(key, node)` —— **只摘出场景树，不 `queue_free`**
- `Pool.clear()` —— 退出时回收全部孤儿节点

接入：`Danmaku`（弹幕）、`Sword`（飞剑）、`Fx`（特效）。三个类各加 `POOL_KEY` 与
`static func spawn(...)` 作为唯一生成入口；`Enemy._shot` / `Boss._b` / `Boss._rain` /
`Player._spawn_sword` 全部改走 `spawn()`。

配套改动：

- `Danmaku` 的 `CollisionShape2D` 只在 `_ready()` 建一次，`setup()` 只改 `radius`
  —— 原来每颗弹都要新建一个节点 + 一个 `CircleShape2D`。
- `setup()` / `reset()` 覆盖全部可变状态（颜色、速度、寿命、旋转、`modulate`、`visible`），
  否则复用时会残留上一颗弹的外形。

> 踩坑 1：`Fx` 里原本想把取节点的辅助函数命名为 `_get()`，编译报
> `The function signature doesn't match the parent. Parent signature is "_get(StringName) -> Variant"`
> —— `_get` / `_set` / `_property_can_revert` 是 `Node` 的虚方法，改名 `_take()` 解决。

> 踩坑 2：Area2D 属于 `CollisionObject`，在物理回调里直接 `remove_child` 会报
> `Removing a CollisionObject node during a physics callback is not allowed. Use set_deferred() instead.`
> —— 弹幕/飞剑迁到 `_physics_process` 后，`_kill()` 正好落在这个回调里。
> 解法：`_kill()` 只做「置死 + 隐藏」，真正的摘除走 `_detach.call_deferred()`。

> 踩坑 3：池内节点是无父节点的孤儿，进程退出时会被统计成
> `20 RID allocations of type 'GodotArea2D' were leaked`。
> 解法：`Game._notification(NOTIFICATION_PREDELETE)` 里调 `Pool.clear()`，
> 自测脚本也在 `get_tree().quit()` 前主动清一次。

**4c 位移迁到物理帧（`Sword.gd` / `Danmaku.gd`）**

飞剑 980 px/s：30fps 下单帧位移 32px，而小妖碰撞半径只有 19px —— 放在 `_process` 里
会直接跨过去造成穿模。两者 `_process` 全部改为 `_physics_process`。
`Player` / `Enemy` / `Boss` 速度较慢（≤340 px/s）且需要跟随渲染帧的平滑度，保持 `_process`。

**4d 严格 GDScript 警告已开启**

`project.godot` 中已写入 6 项（`untyped_declaration` / `inferred_declaration` /
`unsafe_property_access` / `unsafe_method_access` / `unsafe_cast` / `unsafe_call_argument`），
实测 **0 warning**。

## 阶段进度

- [x] **Phase 1** 真 bug 修复
- [x] **Phase 2** 解耦（`world` 注入 + HUD 信号化）
- [x] **Phase 3** 抽象与类型化（Damageable / 信号 / 类型化 Dictionary / DrawUtil）
- [x] **Phase 4** 性能与稳定性（背景烘焙 / 对象池 / 物理帧迁移 / 严格警告）
- [x] **Phase 5** 开始界面（三项主菜单 / 道袍库 / 游戏说明）与「皮肤 → 道袍」术语统一
- [x] **Phase 6** 第四色「戊土」（怪物黄弹 / 戊土符袍 / 持续符光 / 过热机制）
- [x] **Phase 7** 三档难度（简单 / 普通 / 困难）与老祖狂暴机制
- [x] **Phase 8** 漂浮道具与增益系统（回春 / 剑影 / 增攻 / 无量）

回归方式：每阶段跑一次 `运行自测.bat`，**134 项断言全 PASS 且 ERROR / WARNING 计数为 0** 即为通过。

### Phase 8 · 漂浮道具与增益系统（8 文件）

新增 `entities/Pickup.gd`，四种道具（回春丹 +20 血 / 剑影符 弹道 +1 / 增攻符 +30% / 无量罩 6 秒无敌），
两种掉落源：斩妖 18% 概率（N=2000 实测 18.65%）、每重妖潮结束额外刷 **1** 个。
每局约 6~7 个。波次结束**不再回血**（`_heal()` 已移除），元神只靠回春丹补。

**8a 碰撞层 bit4，而且必须是双向的**

`Pickup` 设 `collision_layer = 16`、`collision_mask = 1`。但 Godot 的 Area2D 重叠判定是
**双向**的——`A.layer & B.mask` 与 `A.mask & B.layer` 都要非零。所以只给道具设层不够，
**玩家的 `collision_mask` 也必须加上 16**（现在是 `4 | 8 | 16`）。漏了这步道具会直接穿身而过，
且不报任何错。飞剑（mask=bit2）与敌弹（mask=bit0）都不会误触道具，这是分层的好处。

检测方向选的是**道具认玩家**（`Pickup._on_area_entered` 判 `a is Player`）而不是反过来：
「道具把效果施加给玩家」，责任本来就在道具一侧，Player 不必为每种道具各开一个分支。

**8b 玩家侧的三个状态 + 五个派生访问器**

`multi` / `atk_up` / `invinc` 三个原始状态，其余全部派生：
`rows()` / `sword_damage()` / `sword_size()` / `beam_dps()` / `beam_width()`。
派生而非存值 —— 否则「换袍导致基准排数变了」这类联动会到处漏。

- 排数 = 道袍基准（赤炎 2 / 其余 1 / 戊土一道符光）+ `multi`。
- 齐射间距 `min(34, 100/(n-1))`：**不收窄的话 5 排会铺到 ±68，贴边的剑直接飞出屏幕**。
- 戊土的 `_beam: Beam` 改成 `_beams: Array[Beam]`——原来单道写死，加排就得改结构。

**8c 「增加攻击」必须同时是数值和视觉**

- 飞剑走**节点 `scale`**，碰撞体跟着一起变大。只改 `_draw()` 不改碰撞体的话，
  剑看起来很宽却擦身而过 —— 那是视觉骗人，比不放大更糟。
- 符光的 `width_mul` 同时作用于 `_shape.size` 与 `_draw()` 的四层线宽。
- 两者共用 `ATK_VIS` 一个系数，想调强度改一处。

**8d 剑影符对戊土「不额外升温」要写测试锁住**

`_update_heat()` 只认「是否在出光」这一件事，所以加排天然不加发热。
但这是**需求的明文要求**，不能靠巧合成立 —— 断言方式是 `multi=0` 与 `multi=2`
各出光 1 秒，过热值必须完全相等（实测都是 20）。
否则将来谁在 `_update_heat` 里顺手加一句 `heat += HEAT_RISE * rows() * delta` 就破了，
而那种改动在别处看起来完全合理。

**8e 关卡掉落源**

- 斩妖掉落写在 `_on_enemy_killed()` 里（`randf() < DROP_CHANCE`），因为那是唯一的击杀回调。
- `_drop_wave()` 每波刷 **1** 个（初版是 2 个，见 8i），位置落在**场景右段**而不是玩家脚下 ——
  想要就得挪过去，顺带把「要不要为了道具冒进」变成一个真选择。
- `_stop_field()` 补了 Pickup 清理。
- 每局道具数：斩妖约 3.6 个 + 三波固定 3 个 ≈ **6~7 个**。固定来源只占不到一半，
  「斩妖出不出货」才有存在感。

**8f 概率行为怎么写断言**

掉落是随机的，不能直接断言「掉 / 不掉」。做法：连调 60 次 `_on_enemy_killed()`，
只要命中次数 > 0 就算过 —— 一次都不出的概率是 `0.82^60 ≈ 6e-6`，确定性足够。

拾取那条更容易写成假通过：道具以 58 px/s 向左漂，把玩家放上去干等 20 帧，
它可能已经漂出 32px 的判定范围了。改成**每帧把玩家重新贴到道具位置**再轮询状态，
这样测的是「碰撞与生效链路」而不是「运气」。

> 与 D1 的教训同源：这类用例最怕的是「测试通过了但没测到」。

**8g HUD 与说明界面**

- 增益小牌画在血条下方（弹道 ×N / 攻击 +N% / 无量 N.Ns），只显示生效中的。
- `HelpScreen` 加第七节后超出 720 高：行距 24 → 21、字号 16 → 15，再把「符光与过热」并掉一行才装下。
  纯绘制 UI 没有自动布局，加内容就得重新量一遍高度。

**8h 概率性缺陷必须写成确定性用例（本次最值钱的一条）**

第一轮 107 项全绿、`ERROR` 计数 0，我以为稳了。再跑一次却是 `BAD=4`：

```
ERROR: Can't change this state while flushing queries.
       Use call_deferred() or set_deferred() to change monitoring state instead.
  at: area_set_shape_disabled (godot_physics_server_2d.cpp:354)
  [0] _ready (Pickup.gd)   [1] spawn (Pickup.gd)
  [2] _on_enemy_killed (Level.gd)   [3] _die (Enemy.gd)
  [4] hit (Enemy.gd)       [5] _on_area_entered (Sword.gd)
```

根因：掉落是**在物理回调里**发生的（飞剑命中 → `Sword._on_area_entered` → `Enemy._die` → 关卡掉落），
而 `Pickup._ready()` 会设碰撞层并挂 `CollisionShape2D` —— 物理服务器正在 flush 查询时不许改碰撞体。
规则与 Phase 4 踩过的是同一条：**别在物理回调里动碰撞体**（那次是 `remove_child`，这次是 `add_child`）。

修法：`Pickup.spawn()` 里把 `add_child` 推到帧末（`_enter.call_deferred(parent)`），
并顺手在 `_enter` 里校验父节点是否还活着（关卡没了就别再挂上去）。
放在 `spawn` 里而不是调用点，是为了让**所有**调用方天然安全 —— 谁调用都不需要懂这条物理规则。

**为什么第一轮没抓到**：击杀掉落是 18% 概率，`_test_level` 只打死几只怪，
`0.82^5 ≈ 37%` 的概率整轮一个都不掉 —— 用例本身没问题，是**概率让它时灵时不灵**。
于是补了一条确定性用例：造一对真实重叠的 Area2D，把 `Pickup.spawn` 塞进 `area_entered`
（正是 flushing queries 阶段），不再靠运气。

**并且按老规矩做了撤修复实验**：把 `call_deferred` 改回 `add_child` 再跑 ——
**110 项断言照样全 PASS，日志里却冒出 4 条 ERROR**。
与 D1 的结论完全一致：**断言抓不到，只有「日志无 ERROR」这道闸兜得住**。
（「物理回调里也能安全生成道具」那条断言同样 PASS —— 道具确实生成了，只是顺带报了错。）
详见「已修缺陷 · D2」。

**8i 「道具太多」先量再改，别拍脑袋调概率**

反馈是「道具出现概率过高」。第一反应容易是直接把 18% 砍下去 —— 但**概率可能根本没写错**，
只是来源结构不合理。所以先在自测里挂了个临时统计：重复 2000 次击杀，
实测**掉落 373 次 = 18.65%**，配置值 18%（期望 360，σ≈17），偏差不到 1 个标准差 —— 代码是对的。

真正的元凶是**波次刷新**：每波固定刷 2 个 = 每局白送 6 个，而斩妖掉落期望只有
约 3.6 个（20 只怪 × 18%）。固定来源占了总量六成，「随机掉落」这一层设计被稀释得没有手感，
玩家只觉得「怎么一直在掉」。所以：

| 改动 | 每局道具数 |
| --- | --- |
| 初版（斩妖 3.6 + 每波 2 个 × 3） | ≈ 9.6 |
| 每波 1 个（现状） | ≈ 6.6 |

同时按反馈**删掉了每波结束回复元神**（原 8 / 8 / 10 点，`_heal()` 整个移除）——
有回春丹之后，白送的回血既是重复来源，又让「要不要绕路捡丹」这个选择失去意义。

两条回归锁：自测里加 `_ck(n1 - n0 == Level.WAVE_DROP, "每波结束刷新 1 个道具")` 与
`_ck(not lv.has_method("_heal"), "波次结束不再回血")`。后者是条元断言 ——
它防的不是逻辑错误，而是**将来有人把 `_heal()` 加回来**。

> 这条的经验：遇到「数值不对」的反馈，先花一次测量把「代码写错了」和「配置/结构不合理」
> 分开。前者要修，后者要调，改错了地方下次还会再犯。

### Phase 7 · 三档难度与狂暴（8 文件）

需求拆成两张表：难度只改**老祖的数值与机制**（妖潮一视同仁），狂暴则三档共有。

| | 简单 | 普通 | 困难 |
| --- | --- | --- | --- |
| 老祖元神 | 1000 | 2500 | 3600 |
| 法相 | 两重（50%） | 三重（67% / 34%） | 三重（67% / 34%） |
| 属性法罩 | 无 | 无 | 有 |
| 异色伤害 | 100% | 100% | **60%** |
| 弹幕密度 | 55% | 80% | 100% |

**7a 参数放访问器而不是配置表（`Game.gd`）**

五个带返回类型的函数：`boss_hp` / `boss_phases` / `boss_ward` / `bullet_scale` / `off_color_mul`。
没写成 `const CFG := [{"hp": 1000, ...}]` 是因为 Phase 5 就踩过——
从**未类型化数组**取值 `var x := arr[i]` 推断不出类型，调用点得处处写 `int(cfg["hp"])`，
写漏一个就是运行期类型错误。访问器让调用点直接拿到 `int` / `bool` / `float`，编译器替你兜底。

**7b `max_hp` 取代 `MAX_HP`（`Boss.gd`）**

这是本次最容易漏的一处。原来 HUD 的血条比例、阶段判定的 `r`、狂暴阈值全拿 `MAX_HP` 常量算，
改难度后**血条会按 3600 显示而实际只有 1000** —— 不报错，只是条永远填不满。
新增 `var max_hp`，`_ready()` 里从 `Game.boss_hp()` 取一次，四个难度参数同样只取一次
（`_phases` / `_has_ward` / `_bullet_k` / `_off_color`），之后不再查全局。

**7c 弹幕密度是一个系数管两件事**

`bullet_scale` 既拉长发射间隔（`_tick = TICK[_skill] / _bullet_k`），
又收缩一次齐射的弹数（新增 `_n(base) = maxi(1, round(base * k))`）。
`maxi(1, ...)` 不是保守——简单档 `k = 0.55` 时「玄冰三连狙」的 `round(3 * 0.55) = 2` 还算好，
但再往下的套路会算出 0，直接一发不出。小妖同步：`fire_cd *= 2.0 - k`，所以妖潮也跟着变稀。

**7d 狂暴（`Boss._check_enrage` / `_enrage_fire`）**

血量跌破三成（`ENRAGE_AT = 0.30`）触发，一次性、不解除：

- 视觉：脉动血红光晕 + 血色符环 + 10 颗**逆向**游走的血色符点（与末阶狂气同向的话会看不出区别）。
- 弹幕：四条旋臂各占一色，同角速度外卷，每 `ENRAGE_TICK = 0.09s` 转过 0.40 弧度发一轮。
  间隔同样除以 `_bullet_k`，所以简单档的螺旋也慢。
- 这层螺旋与当前法相的套路**并行**，困难档狂暴后是「法罩 + 套路 + 螺旋」三重压力。
- 触发走 `enrage_started` 信号 → `Level` 放横幅，`HUD` 血条标题转红并标注 `· 狂 暴`。

**7e 择难度界面（`ui/DifficultySelect.gd` 新增）**

三张卡，默认停在**普通**（`_sel = 1`）。键盘 `← →` 切换、`1/2/3` 直达并确认、`Enter` 确认、`ESC` 返回，
鼠标悬停即选中、左键即确认——与 Phase 5 的 `TitleScreen` 同一套写法，
只发 `start_pressed` / `back_pressed`，由 `Main` 决定去向。
流程改为：开始界面 → **择难度** → 择道袍 → 关卡；择道袍的 `ESC` 退回择难度而不是开始界面
（改错了难度得能退回去重选）。

**7f 自测扩张到 77 项**

新增 `_test_difficulty()` 11 条：简单档 1000 血 / 一条阶段刻度 / 全程不展法罩、
困难档 3600 血 / 两条刻度 / 法罩下异色 60% 同色 100% / 跌破三成狂暴 / 螺旋实测 4 色。

> **踩坑（本阶段最值钱的一条）**：headless 下帧率约 **140fps 且抖动很大**，
> 「`await _frames(200)` 当 2 秒用」换算不出稳定的游戏时间——
> 首批断言全挂在 `st=enter`，因为老祖那段约 2 秒的入场根本没走完。
> 解法是**轮询状态而不是数帧**：
> ```gdscript
> for _i in 400:
>     if b._st == "fight":
>         break
>     await get_tree().process_frame
> ```
> 同理加固了 `_test_death` 的「老祖收手」——它在入场期间本来会**空过**。
> 这类用例最危险的地方是：改帧率就假通过，你却以为机制是对的。

### Phase 6 · 第四色「戊土」（13 文件）

**6a 第四色落地（`Game.gd`）**

三色变四色不是加一个枚举那么简单 —— 四张颜色表（`COLOR_MAIN/GLOW/DARK/CORE`）、
`COLOR_CN`、`ROBE_TITLE/DESC/TAG/LORE/STORE`、`ACTION_KEYS` 全部要加第 4 项，
漏一个就是运行时越界。为便于检索，注释里标明了「历史称呼：三色」，避免与新增的戊土混淆。

**6b 戊土（黄）符弹与戊土妖**

- `Danmaku._draw()` 补上 `Game.YELLOW` 分支：**旋转的方形符牌 + 一横一竖符纹**。
  四色必须做到「一眼可分」—— 红火球（圆）/ 蓝冰锥（尖）/ 白灵环（环）/ 黄符牌（方）。
  原实现里 `_draw()` 的 `match` 只有 `RED / BLUE / _`，黄色会静默掉进白色的「灵环」分支 —— 这类
  「不报错但长错样」的缺陷，比崩溃更难发现。
- `Enemy` 增加戊土妖：血 30、射速偏慢、**一次三枚宽散射符弹**，外形是方牌身 + 符纹。
- `Level._spawn_enemy()` 由 `randi() % 3` 改为 `% 4`；Boss 的 `chaos` / `homing` 同改，
  法珠与死亡爆散也由 3 循环扩到 4，并新增 `fan_yellow` / `rain_yellow` 两套弹幕。

**6c 戊土符袍 · 符光（`entities/Beam.gd` 新增）**

符光不是弹丸，所以没有走对象池（全程只有这一道），而是常驻挂在 `Player` 下、开火即 `turn_on()`：

- 碰撞体是 `RectangleShape2D`，`aim(player_x)` 每帧把长度拉到「玩家 → 屏幕右缘」。
- 伤害按 `TICK = 0.2s` 结算给 `get_overlapping_areas()` 里所有 `Damageable`，
  因此是**持续输出**而非命中即消失。

**6d 过热机制（`Player.gd`）**

| 常量 | 值 | 含义 |
| --- | --- | --- |
| `HEAT_MAX` | 100 | 满值即无法出光 |
| `HEAT_RISE` | 20 /s | 出光时累积 |
| `HEAT_COOL_DELAY` | 0.5 s | 停手多久后开始散热 |
| `HEAT_COOL` | 30 /s | 散热速度 |
| `HEAT_VENT` | 30 | 触及戊土弹立刻散去 |
| `HEAT_REARM` | 60 | 闭锁后散到此值以下才重新解锁 |

三处设计要点：

1. `_update_heat()` 与 `_shoot()` 通过 `_firing` 单向通信 —— `_shoot()` 每帧先算能否出光，
   `_update_heat()` 只读结果，避免「过热了还在加温」的时序竞态。
2. 戊土弹的散热写在 `take_hit()` 的**免疫判断之前**，所以「硬吃一发戊土弹换散热」在任何道袍下都成立 ——
   这是戊土符袍的核心节奏点，不能因为换了袍就失效。
3. **必须加 `HEAT_REARM` 这道解锁闸门**。只按字面写「热 >= 100 就锁」的话，玩家按住不放会卡进
   「锁 0.5 秒 → 散热一帧 → 立刻又能开 → 两帧后回到 100 → 再锁」的循环，占空比只剩约 5%，
   符光实际上是废的。加闸门后满一轮的代价是约 1.8 秒停手，而硬吃一发戊土弹能省下其中 1 秒 ——
   机制这才真正立起来。这类问题静态读代码看不出来，是靠把数值代入帧循环推一遍才发现的。

**6e 界面随四色扩容**

`RobeSelect` / `RobeGallery` 原来按三件写死了 `for i in 3` 和居中偏移 `float(i) - 1.0`。
改为 `CARD_N` / `TAB_N` 常量后，居中偏移变成 `(float(i) - (float(N) - 1.0) * 0.5)`，
卡宽与页签宽同步收窄（272 / 194）以塞进 1280 宽。`HelpScreen` 增补「符光与过热」一节后行数超出，
把行距 27 → 24、字号 17 → 16 才装得下 —— 纯绘制的 UI 就是这样，改文案要顺手量高度。

**6f 自测扩张到 58 项**

新增 `SelfTest._test_yellow()`（断言 43 → 57，其中 14 条属本阶段）：戊土免疫、过热 +20 / 封顶 / 停手散热 /
闭锁期间不可出光 / 戊土弹 −30 四条数值链、戊土妖确实射出 3 枚黄弹、符光确实让敌人持续掉血。
符光那条最容易写假：**直接调 `Beam._tick()` 会因为物理体未同步而拿不到重叠区**，
必须 `turn_on()` 后真跑若干物理帧再断言。

> 踩坑：`--import` 后 `update_scripts_classes` 里出现了 `Beam` / `RobeArt` / `RobeGallery` /
> `RobeSelect` 四项新条目 —— 这是 Godot 在注册 `class_name` 全局类，属正常输出而非报错。
>
> 后续：Phase 6 收尾后实机又报 `previously freed`，已作为 D1 修掉，见「已修缺陷」。

### Phase 5 · 开始界面与「皮肤 → 道袍」术语统一（12 文件）

**5a 开始界面三项主菜单（`ui/TitleScreen.gd` 重写）**

原来按任意键直接跳择人界面。现在是一个真正的菜单：

| 项 | 触发 | 去向 |
| --- | --- | --- |
| 开始游戏 | `↑↓` + `Enter`，或数字键 `1` | 择道袍 → 关卡 |
| 道袍库 | 数字键 `2` | `RobeGallery` |
| 游戏说明 | 数字键 `3` | `HelpScreen` |

三个界面都发信号给 `Main`（`start_pressed` / `gallery_pressed` / `help_pressed`），
`Main` 只做 `_clear()` + 新建 + 连接，界面之间互不认识 —— 与 Phase 2/3 的解耦方向一致。
鼠标悬停即高亮、左键点击即进入；键盘与鼠标两套入口共用同一个 `_activate()`。

**5b 新增两个界面 + 一个绘制工具**

- `ui/RobeGallery.gd` —— 道袍库。左侧立绘、右侧「免疫 / 神通 / 来历详述」，
  底部三个页签；`← →` 或 `1/2/3` 翻阅，页签上标出「已择」。
- `ui/HelpScreen.gd` —— 游戏说明。五节（操作 / 四色属性 / 道袍 / 关卡与破罩 / 计分）双列排布。
- `ui/RobeArt.gd` —— **纯静态**道袍立绘工具。
  道袍库要展示形制，但直接 `Player.new()` 会带来移动、开火、碰撞等一整套逻辑，
  所以把修士外形抽成 `RobeArt.draw(ci, pos, color, t, scale)`，任何 `CanvasItem` 都能画。
  择道袍界面的三张卡面也改用它（原来只是个光球）。

**5c 术语与标识符统一（皮肤 → 道袍）**

不只是改文案，代码标识符一并改掉，避免术语分裂：

| 改前 | 改后 |
| --- | --- |
| `Player.skins` / `skin_idx` / `skin_changed` | `robes` / `robe_idx` / `robe_changed` |
| `Boss.player_skins` | `player_robes` |
| `Game.picked` | `Game.picked_robes` |
| `Game.SKIN_TITLE/DESC/TAG` | `ROBE_TITLE/DESC/TAG`（另增 `ROBE_LORE` / `ROBE_STORE`） |
| `ui/SkinSelect.gd`（`class_name SkinSelect`） | `ui/RobeSelect.gd`（`class_name RobeSelect`） |

Boss 的「第 N 重法相」保留 —— 那是 Boss 的形态，不是玩家的道袍，混改反而更难读。

> 踩坑：`HelpScreen` 原本把说明文案写成 `const SECTIONS := [{...}, ...]`，
> 报 `Cannot infer the type of "x" variable` —— 一是 const 里套 Dictionary/Array 不是常量表达式，
> 二是 `var x := col_x[col]` 这种从**未类型化数组**取值也无法推断。
> 解法：文案改由 `static _build_sections()` 在 `_ready()` 里构建；下标取值显式写 `var x: float = ...`。

## 已修缺陷

### D1 · 玩家陨落后 Boss 继续开火 -> `Invalid assignment ... previously freed`

**现象**：玩家在 Boss 战中阵亡，控制台刷
`Invalid assignment of property or key 'target' with value of type 'previously freed' on a base object of type 'Area2D (Danmaku)'`，
栈指向 `Boss._b`。

**根因**：`Player._die()` 会 `queue_free()` 自己，而 `Level._on_player_died()` 之前只是
`_running = false` + `await wait(1.5)`。这 1.5 秒里 Boss 照常按套路开火，
`Boss._b()` / `_rain()` 里那句 `b.target = player_ref` 拿到的已是一个**已释放对象** ——
Godot 在给带类型的属性赋值时会当场校验并报错。

**修法**（两层，缺一不可）：

1. `Boss._live_player()`：所有要交出玩家引用的地方统一走它，无效就给 `null`
   （`target = null` 是安全赋值）。
2. `Level._stop_field()`：玩家陨落时清弹、撤妖、把 `boss.player_ref` 置空并调
   `boss.stand_down()`（新增 `"idle"` 状态，只飘着不再开火）。
   只做第 1 层的话，Boss 会在人死后继续朝空处倾泻弹幕 —— 那本身就是设计错误。

**验证**：`SelfTest._test_death()`。为确认用例有效，我把两层修复**分别撤掉各跑了一次**，
都能精确复现原报错（`_b` 同一行栈）；恢复后 110 项全 PASS、日志 0 ERROR。

> 值得记一笔：这条缺陷的断言其实是「软」的。赋值报错时 Godot 会**跳过赋值**，
> 于是 `target` 反而保持 `null`，`_ck(target == null)` 照样通过。
> 也就是说这类问题**只能靠「日志无 ERROR」这道闸**兜住，断言抓不到。
> 回归时别只看 PASS 计数。

### D2 · 在物理回调里生成道具 -> `Can't change this state while flushing queries`

**现象**：打死小妖触发掉落时，日志报
`ERROR: Can't change this state while flushing queries. Use call_deferred() or set_deferred() to change monitoring state instead.`，
栈是 `Sword._on_area_entered` → `Enemy.hit` → `Enemy._die` → `Level._on_enemy_killed` → `Pickup.spawn` → `Pickup._ready`。

**根因**：`Pickup._ready()` 会设 `collision_layer` 并挂 `CollisionShape2D`，
而这条链路上游是一个**物理回调**（飞剑命中）。物理服务器 flush 查询期间不允许改碰撞体 ——
与 Phase 4「Area2D 不能在物理回调里 `remove_child`」是同一条规则，只是这次是 `add_child`。

**修法**：`Pickup.spawn()` 把入树推到帧末（`_enter.call_deferred(parent)`），
并在 `_enter` 里校验父节点是否还活着。修在 `spawn` 里而非调用点，所有调用方天然安全。

**两个值得记的点**：

1. **第一次跑没抓到**。击杀掉落是 18% 概率，一轮测试只打死几只怪，约 37% 的概率整轮不掉。
   所以补了确定性用例（一对重叠 Area2D 把 spawn 塞进 `area_entered`）——
   **概率性缺陷不能靠概率去覆盖**。
2. **撤掉修复后 110 项断言照样全 PASS**，只有日志里的 ERROR 暴露问题。
   与 D1 同型：断言 PASS ≠ 没缺陷。

## 可选的后续（非阻塞）

1. `Player` / `Enemy` / `Boss` 的绘制仍是逐帧 `queue_redraw()` + 程序化绘制，
   若将来目标平台是低端移动端，可把不变的部件（道袍轮廓、Boss 躯干）也烘焙成纹理。
2. 对象池目前按 `String` 分桶、上限 512；若要更严格可改为按关卡重置。
3. `Level` 的波次节奏靠 `await create_timer` 串联，暂停时定时器不会停
   （依赖 `process_mode`），目前 HUD 暂停只对节点生效，可考虑统一走 `SceneTree.paused` 的计时器。
