# 10 · 交接与待合并清单

> **状态：题材重构代码侧已暂停写入**（2026-09-22 12:50 用户裁定）。
> 原因：项目上存在另一个由用户本人在另一会话并行运行的「五关改造」工作流，与我们改的是同一批文件。
> 本文档记录**已完成什么、什么被覆盖、基线锁定后要补什么**，供后续直接接续。

---

## 一、为什么暂停

「五关改造」在 12:03–12:51 期间持续活跃写入：

| 文件 | 创建 / 修改时间 | 归属 |
|---|---|---|
| `docs/architecture/五关改造架构.md` | 创建 12:03，修改 12:45 | 对方 |
| `scripts/StageCfg.gd` | 创建 12:13，修改 12:27（untracked） | 对方 |
| `scripts/entities/EnemyKind.gd` | 创建 12:14（untracked） | 对方 |
| `scripts/BossCfg.gd` | 创建 12:36（untracked） | 对方 |
| `scripts/ui/StageSelect.gd` | 创建 12:40（untracked） | 对方 |
| `scripts/world/Level.gd` | 修改 12:48 / 12:51 | **双方重叠** |
| `scripts/entities/Boss.gd` | 修改 12:51 | **双方重叠** |

题材重构侧文档 `01-术语映射表.md` 创建于 10:46，两条线并行约两小时。

**关键教训**：工程跑出的门禁结果会随对方写入而失真。实测 12:49 读到 `Level.gd:492` 是 `Enemy.new()`，12:51 工程读到的是 `Spawner.enemy(...)` —— 同一处代码两分钟内变了两次。**在对方停手前，任何门禁/取帧结论都不可信。**

---

## 二、已完成且安全（非重叠，对方未触碰）

这些文件的改动**已落盘、不会丢**，且不因暂停失效：

| 文件 | mtime | 内容 |
|---|---|---|
| `scripts/art/PirateArt.gd`（原 `YokaiArt.gd`） | 12:27 | 类名 `PirateArt`；`draw_elite()` 精英机机械底盘重画；死常量 `ELITE_BODY_*` / `ELITE_NAILS_YELLOW` 已删 |
| `scripts/entities/Player.gd` | 12:24 | 人形机甲；**CANOPY 驾驶舱玻璃**（敌我识别主通道，朝 +X）+ CORE 座舱高光 + 前向探针 |
| `scripts/ui/ArmorArt.gd`（原 `RobeArt.gd`） | 12:24 | 与 `Player._draw()` 视觉一致（同款 CANOPY 舱盖） |
| `scripts/world/Background.gd` | 12:30 | 星域化；新增 `_make_nebula()` 星云烘焙层、小行星带、主行星、星际尘埃；指令预算 ≈103（≤105） |
| `scripts/entities/Danmaku.gd` | 12:26 | 母题语义：火球→等离子团 / 冰锥→低温晶锥 / 灵环→**相位环** / 符牌→**引力井牌**（PNG 不动，仅注释） |
| `scripts/art/ArtAssets.gd` | 12:27 | `"boss"` 键已删除（配合 Boss 撤图） |
| `scripts/Game.gd` | 12:23 | 新增 `const CANOPY := Color(0.62, 0.86, 0.98)` |
| `_selftest/SelfTest.gd` | 12:45 | 新增 `_test_vector_static()`（V0~V4 矢量静态检查，含注释剥离器）；UI 流程按 5 关流修正；「始祖 / 袍」描述串清理 |

此外 RT-03（早前，门禁曾全绿 fails=0 / ERROR=0 / WARNING=0）已完成：
- 类与文件重命名 `RobeSelect→ArmorSelect`、`RobeGallery→ArmorGallery`、`RobeArt→ArmorArt`
- 标识符 `robes→armors` / `robe_idx→armor_idx` / `robe_changed→armor_changed` / `picked_robes→picked_armors` / `ROBE_*→ARMOR_*`
- `project.godot`：`config/name` → **星际弹幕**
- 品阶串：金勋 · 星帅 / 银勋 · 星将 / 铜勋 · 星尉 / 铁勋 · 星兵（**· 两侧带空格**）
- 四色：`电浆 / 寒霜 / 光子 / 引力`（`COLOR_CN`，色值与枚举 0-3 未动）

---

## 三、已发生实际覆盖（需合并时修复）

### `scripts/entities/Boss.gd` —— 冲突最严重

| 位置 | 现状 | 处置 |
|---|---|---|
| 头部 3 行 | 「关卡 Boss · 母舰核心」✅ 我方成果幸存 | 保留 |
| 593 / 611 行 | 矢量核心几何 `_draw_core()` ✅ 我方成果幸存 | 保留（外环桁架 + 反应堆芯 + 舰桥塔，`r ≤ 75`，保留 `17/11` 反应堆节点尺寸差，全路径禁 CANOPY） |
| **606 行** | 注释被回退为「**星盗始祖**本体（矢量）」 | **改回「母舰核心」** |
| **141–142 行** | 对方新增 `title = "星盗始祖"` / `boss_name = "星盗始祖"` | **改为「母舰核心」** —— 这是硬红线，会触发 09 验证方案的修仙词扫描 |

> 「始祖」是「血魔老祖」的直译，在「星盗 + 无人兵器」的机械阵营里语义不通，且已被 08 终裁明令弃用。

### `scripts/world/Level.gd` —— 对方重构中

我方早期写入的母舰核心 banner 文案处于对方数据驱动重构的改写范围内。**等对方停手后重新核对**，不要现在改。

### 待核实（可能重叠）
`scripts/Game.gd`、`_selftest/SelfTest.gd` —— 需确认对方是否也改过。

---

## 四、基线锁定后的待办清单

按依赖顺序：

1. **确认对方停手**：`git status` + 关键文件 mtime 连续两次快照不变。
2. **修复覆盖点**：`Boss.gd` 141-142 / 606 的「始祖」→「母舰核心」。
3. **核对重叠文件**：`Level.gd`、`Boss.gd`、`Game.gd`、`SelfTest.gd` 里我方题材词是否被再次覆盖。
4. **复跑自测门禁**：`运行自测.bat`，标准 **fails=0 / ERROR=0 / WARNING=0**（不卡 PASS 条数，会漂移）。
5. **补齐取帧脚手架**：`_capture/` 下没有 `Capture.tscn`（只有 CaptureEnemy / CaptureLevel / CaptureUI，产物 `_rollback_*.png`）。已授权 quality-lead 新建 Capture / CapturePair / CaptureBoss，产物放 `D:/demo/_qa/`。
6. **实机取帧**（必须真窗口，`--headless` 是 dummy 驱动拿不到视口纹理），逐条确认美术点名的三条残余风险：
   - ① CANOPY 约 50px，玩家与精英机同屏相距 200/300/400px 时余光下能否立刻分辨敌我
   - ② 玩家电浆型**双炮管** vs 精英机**主炮**是否互读（靠"对称 vs 非对称"区分）
   - ③ Boss 画面已是**母舰核心**而非人形老祖
7. **执行 09 验证方案**：术语扫描（注意 A 类词「重」必须带限定词 `第\s*[一二三1-3]\s*重|每重|三重|两重|重法相`，不能裸搜）、13 组成对概念核对、静默过时用例排查。
8. **遗留文案修正**（quality-lead 实测发现）：
   - `血光` → `赤光`（Boss 无生物化）
   - `十息` → `十秒`（**需先核对 `Player.SHIELD_REGEN` 实际秒数**）
   - `Level.gd:190/199/208/209`、`HUD.gd:264`、`SelfTest.gd:888-892` 的「第 N 重 / 两重 / 三重」→「第 N 波 / 两波 / 三波」
   - **HelpScreen 底部提示当前已重叠**：右列末行基线 y=681，底部提示 y=690，重叠约 6~12px，需压缩至末行基线 ≤670
   - `ArmorArt.gd:49` 注释「3 枚」但循环 `for i in 4`；`ArtAssets.gd:13` 注释「r / 9」但 `ART_BASE_R` 已是 17
9. **`export_presets.cfg`** 产物名改「星际弹幕.exe」（用户已拍板：**只改配置，不重导出**；目录路径保持 `D:\demo\xiuxian-danmaku\build\`）。

---

## 五、已裁定的边界（后续勿再翻案）

- **改**：代码标识符题材词（`ArmorSelect` / `armors` / `armor_idx` / `ARMOR_*` / `PirateArt` 等）
- **不改**：资产文件名 `robe_*`。`enemy_*` `elite_*`、`ArtAssets` 键 `"robe"`、`tools/build_robes.py` 的 `OUT_DIR`（死资产，用户裁定保留原名）
- **死资产说明**：`robe_*` / `enemy_*` / `elite_*` 运行时**不加载**（角色外观已回滚为矢量绘制）。针对它们的像素断言属静默过时用例，须删除或改判据。
- **`tools/build_robes.py` 的 `SKIN` 不用改**：要改的是 `Player._draw()` / `ArmorArt.draw()` 的矢量色。
- **品阶串带空格** `金勋 · 星帅`。

---

## 六、交付文档索引（`design/sci-fi-retheme/`）

| 文档 | 作者 | 内容 |
|---|---|---|
| `01-术语映射表.md` | design-strategist | 核心映射 + 改/不改标识符 |
| `02-替换文案清单.md` | design-strategist | 逐文件成品中文（含字数增减） |
| `03-标识符重命名清单.md` | design-strategist | 含文件重命名 + 不动清单 + 存档方案 |
| `04-文案风格指南.md` | design-strategist | 语气基调、术语规范、修仙词黑名单 |
| `05-一致性自检清单.md` | design-strategist | 10 组成组核对 + 扫描命令 |
| `06-美术规格.md` v3.1 | art-director | 视觉身份、配色、敌我识别 |
| `07-资产规格.md` v3.1 | art-director | 逐资产规格、断言、尺寸红线、残余风险 |
| `08-命名终裁.md` | design-strategist | Boss / 杂兵 / 精英最终命名 |
| `09-验证方案.md` v2 | quality-lead | 术语扫描、成对核对、取帧清单、判定门槛 |
| `10-交接与待合并清单.md` | team-lead | 本文档 |
