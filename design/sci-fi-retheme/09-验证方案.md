# 09 · 题材重构验证方案（quality-lead 严守真 · RT-05）

> **⛔ 基线更新（RT-07 / team-lead 终裁，跑扫描前必读）**：本文件正文里的 Boss 名「母舰核心 / 核心」「星盗始祖」、杂兵「星盗无人兵器 / 星盗机兵」、类名「YokaiArt」**均为旧基线，已作废**。
> - Boss 统称 = **星盗旗舰**（紧凑「旗舰」）；五关代号 熔核号/霜噬号/耀斑号/深渊之喉/终焉号，称号 星盗先驱/**星盗霜舰**/星盗炮垒/星盗母舰/星盗王。
> - 杂兵玩家可见串 = **星盗喽啰**；「无人兵器」降为文档/注释层形态描述词。
> - 「母舰核心 / 星盗始祖 / 巢舰核心」进硬黑名单（应命中 0）；「YokaiArt」已改名「PirateArt」。
> - **扫描期望值、组 4、§4.7 帧 E、§3 术语表一律以 `11-五关文案对齐.md` 为准**（本文件正文未逐处重写，避免破坏验证逻辑记录）。L2 称号撞名已裁定 = 星盗霜舰（见 11 §3）。
> - **唯一现行基线**：**以 `11-五关文案对齐.md` 为唯一现行基线**；`01/02/03/08` 中与之冲突的条目一律作废，正文保留作历史追溯。扫描基线认这一句就不会跑偏。

> 状态：**方案已定稿，待 team-lead 下达执行指令后运行**。本轮只产出文档，未执行扫描 / 自测 / 取帧。
> 配套：`04-文案风格指南.md`（黑名单与排版约束）、`05-一致性自检清单.md`（成组项）、
> `06-美术规格.md` §4（敌我识别）、`07-资产规格.md` §2（断言两层方案）§6（残余风险）、
> `08-命名终裁.md`（中文终裁值）。

> **修订记录**
> - **v2（team-lead 裁定后）**：§0.1 验证根目录**由 dev 更正为 master**（我 v1 判错，见该节教训记录）；
>   Q2~Q7 全部裁定并入文档；新增 §0.1-a 时效性纪律；
>   Q3「血光→赤光」、Q4「十息→十秒」移入 A 类硬黑名单；Q5 已授权建取帧脚手架（产物改到 `D:/demo/_qa/`）；
>   Q6 文档层纳入扫描但不判 FAIL。

---

## 0 · 前置事实与必须先解决的阻塞项

### 0.1 ✅ 验证根目录 = `D:/demo/xiuxian-danmaku`（master）

项目确为**双 worktree**，但**施工发生在 master**：

```
git worktree list
  D:/demo/xiuxian-danmaku  651e689 [master]   ← ✅ 验收对象（本方案所在）
  D:/demo/xiuxian-dev          651e689 [dev]      ← 仅 .import 变动，非施工仓
```

实测（team-lead 纠正后我复核的当前状态）：

| 仓 | `git status` |
| --- | --- |
| **master** | `M` ×11：`Game.gd`、`Boss.gd`、`Elite.gd`、`Enemy.gd`、`DifficultySelect.gd`、`HUD.gd`、`HelpScreen.gd`、`ResultScreen.gd`、`TitleScreen.gd`、`Level.gd`，**外加 `YokaiArt.gd → PirateArt.gd` 重命名（含 .uid）** |
| dev | 只有 `.import` ×21（bg_mountains / boss / danmaku_* / elite_* / enemy_* / robe_* / fonts / icon） |

dev 仓那批 `.import` 是 **Godot 打开项目时产生的导入缓存**，不是源码施工。

> ⚠️ **本条我第一版判错了，记录教训**：我第一次 `git status` 时 master 恰好还是干净的（engineering-lead 尚未落盘），
> 我据此把 dev 当成了施工仓。**根因是拿一次性快照当结论。**
> → **新增时效纪律（§0.1-a）**：每轮执行前必须先重新取 `git status` / `git log -1` 快照并写进报告。

> 本方案里所有路径一律写 `D:/demo/xiuxian-danmaku/`。

### 0.1-a 时效性纪律（因 §0.1 的教训新增，执行时必须遵守）

1. **每次扫描 / 跑门禁 / 取帧之前**，先跑一次
   `git -C D:/demo/xiuxian-danmaku status --short` 与 `git -C D:/demo/xiuxian-danmaku log --oneline -1`，
   把输出**原样贴进验证报告头部**（含时间戳）。
2. **不要引用我这份文档里的行号做结论**——行号会漂。所有核对点都写成「取位置命令 + 期望值」，现场重取。
3. 报告里出现「读码时发现」与「执行时实测」两类信息时**必须分开标注**，前者只作定位线索，不作判定依据。
4. 若执行期间 engineering-lead 仍在提交，报告须注明**基准 commit**，超出基准的改动另行复验。

### 0.2 ✅ 已授权（原阻塞项 P0-A）：`res://_capture/Capture.tscn` 目前不存在

team-lead 交办里给的取帧命令是
`Godot_console.exe --path <项目> --quit-after 1500 res://_capture/Capture.tscn` → 产物 `_frame_{a,b,c}.png`。

实测 `_capture/` 下**只有三个别的场景**：

| 现有场景 | 产物 | 内容 |
| --- | --- | --- |
| `CaptureEnemy.tscn` | `_rollback_check.png` / `_rollback_elite_check.png` | 四色杂兵一排 + 一只战将 / 四色战将一排 |
| `CaptureLevel.tscn` | `_rollback_level.png` | 真跑关卡 4.0 秒取一帧 |
| `CaptureUI.tscn` | `_rollback_ui_select.png` / `_rollback_ui_gallery.png` | 择甲界面 / 战甲库 |

**没有 `Capture.tscn`，也没有任何脚本会写 `_frame_a/b/c.png`。**
→ ✅ **team-lead 已授权我在执行阶段新建**（Q5）。规格、约束与产物路径见 §4.1。
本轮（v2）仍不建——等执行指令后一次性建好并回报。

### 0.3 ⚠️ 阻塞项 P0-B：敌我识别主通道（CANOPY）**尚未落地**（RT-04 核心任务之一）

`06` §4.2 把「舱盖色的有无」定为敌我识别**唯一主通道**，`07` §2.4 的 V1 是它的活体保证。**读码时（RT-04 施工前）的现状**：

| 期望 | 读码时状态 |
| --- | --- |
| 玩家侧出现 `CANOPY (0.62, 0.86, 0.98)` | ❌ **0 处命中**（`scripts/` 下 grep `0.62.*0.86` / `CANOPY` 无玩家侧结果） |
| 玩家是 `robe_*.png` 像素精灵（`07` §2 断言前提） | ❌ **玩家已改纯矢量**：`Player.gd` 无 `make_sprite`，`_draw()` 里 `战甲本体（矢量画法）` 手绘六边形；头部用 `draw_circle(..., k)`（`COLOR_CORE`，**不是 CANOPY**） |
| `tools/build_robes.py:98` 的 `SKIN` | ➖ **不用改**（team-lead 裁定）：该脚本产出的是死资产 PNG，本次不跑不改、保留原样。**要改的是 `Player._draw()` / `ArmorArt.draw()` 的矢量绘制色。** |

team-lead 已确认这条是 **RT-04 的核心任务之一**（玩家侧 CANOPY 色 / 敌方禁 CANOPY / Boss 全绘制路径禁 CANOPY 均在施工中），
并已采纳我 §3 的 **S2-b（P1/P2/P3 矢量侧静态检查）作为验收依据**。
→ 执行取帧前**必须先确认 CANOPY 已落地**，否则帧 D（主通道三档距离）**无意义**。

**连带结论（本方案最重要的一条）**：`07` §2.1 的 PNG 断言 **A / D / E / F / G / H 全部失去活体意义**——
它们验的是 `robe_*.png`，而 `ArtAssets.FILES` 已不再注册 `robe` 键（`ArtAssets.gd` 实测只剩 `boss / danmaku_* / sword_player / fx_burst / bg_mountains`），`robe_*` 是彻底死资产。
**照 07 §2 原样实现断言 = 造一批永远 PASS 的假绿。** 处置结论见 §3 S2。

### 0.4 其余已采信事实（不再核实）

- 死 PNG（`robe_*` / `enemy_*` / `elite_*`）保留原名不删（team-lead 裁定）。
- 四色终裁：电浆 / 寒霜 / 光子 / 引力（0/1/2/3），色值不动。
- 终裁：势力=星盗、杂兵=星盗无人兵器、精英=星盗战将、Boss=母舰核心、波次=星袭、玩家=人形机甲。
- 门禁 = `运行自测.bat`（`--headless --quit-after 4000 res://_selftest/SelfTest.tscn`），标准 **fails=0 且 ERROR=0 且 WARNING=0**；**不卡 PASS 条数**（`_ck()` 分支依赖随机，近期约 134，会漂移）。
- `--headless` 是 dummy 驱动，拿不到视口纹理；取帧必须开真窗口。
- Godot 在赋值校验失败时**跳过赋值**而非落入坏值 → 只看 PASS 不够，**必须同时查 ERROR/WARNING 计数**。

---

## 1 · 术语扫描方案

### 1.1 扫描范围与排除集

**纳入**：`scripts/**/*.gd`、`_selftest/**/*.gd`、`_capture/**/*.gd`、`scenes/*.tscn`、`project.godot`、`tools/*.py`。

**文档层**（✅ **已裁定 Q6：纳入扫描、单列清单，但不作 FAIL 门槛**）：
`README.md` / `工程审查报告.md` / `playtest.html` / `docs/**` / `design/**`。
游戏内文本是验收对象，仓库文档不是；但这些文件里修仙词很多（README 实测约 81 处命中），
仍需**扫出清单交用户定夺是否一并改**。判定栏一律写 `[DOC]`，不计入 PASS/FAIL。

**排除**（不扫）：

| 排除项 | 理由 |
| --- | --- |
| `assets/sprites/{robe,enemy,elite}_*.png` 及其 `.import` | 死资产，team-lead 裁定保留原名 |
| **死资产工具链** `tools/build_robes.py` / `build_enemies.py` / `_check_robe.py` / `build_sprites.py` | 本次**不跑不改**（team-lead 裁定）。命中记 `[DEAD-TOOL]`、**不判 FAIL**；但 `_check_robe.py` 本身按 §3 S1 应删除或改判据 |
| `.godot/` `build/` `.git/` `*.uid` `*.log` `_selftest_run.log` | 产物/元数据 |
| `export_presets.cfg` / `运行*.bat` / 目录名 `xiuxian-danmaku` | 用户硬约束，不动 |
| `design/sci-fi-retheme/09-验证方案.md`（本方案自身） | 自指——黑名单就写在这里，扫必命中 |

### 1.2 A 类 · 硬黑名单（命中 = 题材残留，须清零或走白名单豁免）

> 来源：`04` §5 全表 + `08` §2.3 + 我读码时补发现的残留（`灵光`/`本命`/`发髻`/`护体`/`诸法`/`引力符`/旧品阶串/`每重`）。

```
修仙 修真 修士 道人 真人 元神 气血 真元 灵气 灵力 灵环 灵石
道袍 法衣 飞剑 御剑 剑诀 本命飞剑 本命
符光 符箓 符牌 符袍 剑影符 增攻符 回春丹 无量罩 引力符
赤炎 玄冰 太清 戊土 火行 水行 土行 金行 五行
小妖 妖物 妖将 护法妖将 妖潮 妖王 妖怪 魔修 妖
血魔老祖 老祖 始祖 巢舰 巢舰核心
法罡 法罩 法相 法珠 法眼 魔道法相 罡气 罡
问道 道成 道消 此劫 渡劫 入劫 入定
回春 增攻 无量 诸法 护体
元婴 金丹 筑基 炼气 天品 地品 玄品 黄品
发髻 灵光 血光 十息
第\s*[一二三1-3]\s*重  每重  三重  两重  重法相
```

> **Q3 / Q4 裁定后的两项增补（已生效）**：
> - **`血光`** → ✅ 裁定改「**赤光**」（同为 2 字，不影响排版）。理由：「血」带生物联想，与 `08` §1「Boss 严守无生物化词汇」冲突。
>   期望值：`Level.gd:418` 应读作「母舰核心周身泛起**赤光** · 四色螺旋弹幕」。**`血光` 命中即 FAIL。**
> - **`十息`** → ✅ 裁定必须消失，终裁表述「**十秒**」。
>   ⚠️ **但替代表述有前置依赖**：team-lead 未 100% 确认机制时长真是 10 秒，已要求 engineering-lead 核对 `Player.SHIELD_REGEN` 的实际秒数；
>   **若不是 10 秒，则由 engineering-lead 报实际值、design-strategist 重新定措辞**。
>   → 我的判法：扫描按「**`十息` 必须消失**」判（A 类，命中即 FAIL）；**具体替换成什么串不设期望值**，只核对「改后文案与实际秒数一致」。

### 1.3 B 类 · 灰区词（命中 **不自动判失败**，列出交 design-strategist / team-lead 裁定）

| 词 | 现状位置（读码时） | 争议点 | 裁定 / 我的倾向 |
| --- | --- | --- | --- |
| ~~**血光**~~ | `Level.gd:418` | 与 `08` §1「严守无生物化词汇」冲突 | ✅ **已裁定 → 改「赤光」**，移入 **A 类（FAIL）**。见 §1.2 |
| ~~**十息**~~ | `Game.gd:67,75`、`HelpScreen.gd:38`、`Player.gd:17,321,355`、`HUD.gd:206` | 「息」是修仙时间单位 | ✅ **已裁定 → 必须消失**（终裁「十秒」，实际秒数待 engineering-lead 核）。移入 **A 类（FAIL）**。见 §1.2 |
| **息**（裸字） | 同上改完后应无 | 裸搜会误伤「休息 / 利息 / 息屏」等 | **灰区**：只在「十息」清零后仍有裸命中时才报，交人工判 |
| **剑** | `Game.ARMOR_TITLE[0]="电浆剑甲"`、`Pickup.CN[1]="刃影模块"` | 「剑」在终裁战甲名里，非修仙词；但 `04` §5 把「剑影」列入黑名单 | **白名单**：仅当出现在「电浆剑甲」内时豁免；单独的「剑影」按黑名单 |
| **身法** | `HelpScreen` 操作节「移动身法」、`ARMOR_TAG[1]="身法 +50%"` | `04` §2 与终裁文案**主动采用** | **白名单**（新术语） |
| **道** | `DifficultySelect.gd`「初入此**道**」 | 「此道」有修仙味，`04` 列了「问道/道成/道消」但未列「此道」 | 建议保留（通用「门道」义），登记在案 |
| **先锋** | `Level.gd:102`「第 三 波 · **先锋**」 | 军事通用词 | **白名单** |
| **出刃 / 破罩 / 星袭 / 星币 / 护罩 / 力场 / 阶段** | 全局 | `04` §2 / §3 定义的**新术语**，最容易被误判成残留 | **硬白名单** |

### 1.4 C 类 · 英文标识符残留

```
Yokai|yokai|YokaiArt|ROBE_|robe_idx|picked_robes|player_robes|robes\b|RobeSelect|RobeGallery|RobeArt|robe_changed
```

**白名单（C 类唯一豁免）**：存档 JSON 键 `"robes"`——`Game.gd:122`（结构注释）与 `Game.gd:268`（`save_highscore` 写盘）。
`03` B 节明确：「键名改动会导致旧 `highscores.json` 读不出」，**必须保留**。
注：`ArtAssets` 键 `"robe"` 已在现代码中**彻底消失**（FILES 表不再注册），故不再需要豁免。

### 1.5 白名单汇总（命中 → 记入报告「已豁免」栏，**不判失败**）

1. 存档 JSON 键 `"robes"`（`Game.gd:122,268`）——旧存档兼容。
2. 死 PNG 文件名与 `tools/build_robes.py` / `build_enemies.py`（含内部 `SKIN`/`OUT_DIR` 常量）——裁定保留。
3. 终裁新术语：身法 / 出刃 / 破罩 / 星袭 / 星币 / 护罩 / 力场 / 阶段 / 母舰核心 / 星盗战将 / 无人兵器 / 星盗机兵。
4. 终裁战甲名中的「剑」（电浆剑甲）与道具名「刃影模块」。
5. `Sword` 类名 / `POOL_KEY="sword"` / `sword_player` 纹理键（`03` B 不动清单）。
6. 目录名 `xiuxian-danmaku`、`export_presets.cfg`、三个 `.bat` 内路径（用户硬约束）。
7. `difficulty` 相关 `DIFF_CN`「简单/普通/困难」——通用词。
8. **Q3/Q4 的新增期望串**：「**赤光**」（= `血光` 的 2 字等长替换）、「**十秒**」（= `十息` 的终裁替换，实际秒数以 `Player.SHIELD_REGEN` 为准）——这两个是**新词不是残留**，扫描器须放行。

#### 1.5-a 正向期望串（双向断言，quality-lead 必跑）

> **扫描器不能只跑「黑名单未命中」的消极断言**——那只证明「没有错词」，发现不了改错字（如「血光」被写成「红光」，照样不命中黑名单，但也不是终裁值）。
> 故对关键终裁句，须加**正向断言**：既无黑名单词，又**确实出现**下列终裁串。建议扫描器对每条做「子串存在」校验。

| 位置 | 正向期望串（须逐字命中） | 字数 | 说明 |
|---|---|---|---|
| `Level.gd` 狂暴横幅副标题 | `周身泛起赤光 · 攻势全面升级`（前接 `%s` = Boss 名，动态） | 「赤光」2 字等长替「血光」 | 双向：无「血光」+ 有「赤光」 |
| `HelpScreen.gd:60` 护罩泛指句 | `部分旗舰会展开【属性护罩】，护罩色只从你所携两件战甲中抽取` | 29 字（等长） | 双向：无「星盗旗舰会展开」全称断言 + 有「部分旗舰会展开」终裁句 |

> 这两条是 RT-07 新增/修正的可见串，是玩家直接读到的，须最高优先级断言。其余终裁串（始祖→旗舰 等）见 11 §4。

### 1.6 可直接执行的扫描命令

> ⚠️ **本机 `rg` 不在 PATH**（实测 `bash: rg: command not found`，`C:\Users\junhewang\.workbuddy\binaries\` 下无 ripgrep）。
> 三选一，按可靠性排序。

**① 首选 · Python 扫描器（推荐，输出带白名单分类与分级）**
写到项目**之外**的 `D:/demo/_qa/scan_theme.py`（不污染仓库、不改任何 `.py`），逐文件 UTF-8 读、按 A/B/C 三级词表 + 白名单正则匹配，输出：

```
[FAIL]    scripts/world/Level.gd:400          始祖            _set_wave("星盗始祖")
[FAIL]    scripts/Game.gd:67                  十息            十息无伤 · 护盾自动回满
[GRAY]    scripts/Game.gd:63                  剑              电浆剑甲（白名单 4 · 终裁战甲名）
[OK-WL]   scripts/Game.gd:268                 robes          存档 JSON 键（白名单 1）
[DOC]     README.md:412                       修仙           文档层（不判 FAIL）
```
结尾打印 `A类命中 N / B类灰区 M / 已豁免 K / 文档层 D`。

**② 备选 · 内置 Grep 工具（自带 ripgrep，已验证可用）**，按下面正则分批跑：
```
# A 类（第一批 · 生物/修仙主体）
修仙|修真|修士|道人|真人|元神|气血|真元|灵气|灵力|灵环|灵石|道袍|法衣|飞剑|御剑|剑诀|本命|符光|符箓|符牌|符袍|剑影符|增攻符|回春丹|无量罩|引力符
# A 类（第二批 · 四色旧名 / 妖 / Boss）
赤炎|玄冰|太清|戊土|火行|水行|土行|金行|五行|小妖|妖物|妖将|护法妖将|妖潮|妖王|妖怪|魔修|妖|血魔老祖|老祖|始祖|巢舰
# A 类（第三批 · 法/罡/劫/品阶/发髻/护体/血光/十息）—— Q3/Q4 裁定后新增后两词
法罡|法罩|法相|法珠|法眼|魔道法相|罡气|罡|问道|道成|道消|此劫|渡劫|入劫|入定|回春|增攻|无量|诸法|护体|元婴|金丹|筑基|炼气|天品|地品|玄品|黄品|发髻|灵光|血光|十息
# A 类（第四批 · 「重」必须带限定词，裸「重」不可用）
第\s*[一二三1-3]\s*重|每重|三重|两重|重法相
# B 类灰区（血光/十息 已移入 A 类，此处只留需人工判的）
息|身法|出刃|破罩|此道|先锋|剑
# C 类英文
Yokai|ROBE_|robe_idx|picked_robes|player_robes|RobeSelect|RobeGallery|RobeArt|robe_changed|\brobe
```
glob 限定 `*.gd` / `*.tscn` / `project.godot` / `*.py`，路径 `D:/demo/xiuxian-danmaku`。
文档层单独跑一遍（只列不判）：glob `*.md` / `*.html`，限定 `README.md` `工程审查报告.md` `playtest.html` `docs/` `design/`（排除 `09-验证方案.md`）。

**③ 若日后装了 ripgrep**（可直接贴给 engineering-lead 自查）：
```bash
cd /d/demo/xiuxian-danmaku
rg -n --glob '!design' --glob '!docs' --glob '!assets' --glob '!.godot' \
   -e '修仙|修真|修士|道人|真人|元神|气血|真元|灵气|灵力|灵环|灵石|道袍|法衣|飞剑|御剑|剑诀|本命|符光|符箓|符牌|符袍|剑影符|增攻符|回春丹|无量罩|引力符' \
   -e '赤炎|玄冰|太清|戊土|火行|水行|土行|金行|五行|小妖|妖物|妖将|护法妖将|妖潮|妖王|妖怪|魔修|妖|血魔老祖|老祖|始祖|巢舰' \
   -e '法罡|法罩|法相|法珠|法眼|魔道法相|罡气|罡|问道|道成|道消|此劫|渡劫|入劫|入定|回春|增攻|无量|诸法|护体|元婴|金丹|筑基|炼气|天品|地品|玄品|黄品|发髻|灵光|血光|十息' \
   -e '第\s*[一二三1-3]\s*重|每重|三重|两重|重法相' \
   scripts _selftest _capture scenes project.godot tools
# 文档层（只列清单，不判 FAIL）
rg -n -c '修仙|修士|元神|道袍|飞剑|妖|老祖|始祖|法相|法罩|法罡|灵石' \
   README.md 工程审查报告.md playtest.html docs design --glob '!09-验证方案.md'
```

### 1.7 扫描结果判定

| 结果 | 判定 |
| --- | --- |
| A 类命中出现在**玩家可见字符串**（`DrawUtil.txt` 参数 / `show_banner` 参数 / `const CN|CN[TIP]` / `project.godot` 的 `config/name|description` / 波次横幅） | **FAIL**（Blocking） |
| A 类命中只在**注释** | **CONCERNS**（Major，须清零方可 PASS） |
| A 类命中全部落在 §1.5 白名单 | PASS（记入豁免栏） |
| B 类灰区命中 | 登记，交人工判（不自动 FAIL） |
| C 类命中（除 JSON 键 `"robes"`） | **FAIL**（Blocking）——说明标识符改名漏项 |
| 文档层命中（`README.md` / `工程审查报告.md` / `playtest.html` / `docs/` / `design/`） | **不判 FAIL**——单列 `[DOC]` 清单，交用户定夺（Q6 裁定） |
| 死资产工具链命中（`tools/build_*.py` / `_check_robe.py`） | **不判 FAIL**——记 `[DEAD-TOOL]`（team-lead 裁定本次不跑不改） |

---

## 2 · 成对 / 成组一致性核对表（13 组）

> 用法：每组给「**唯一数据源**」+「**期望值**」+「**取出现位置的命令**」。
> 拿到位置清单后**逐条比对期望值**，任一处分叉即记 CONCERNS（注释）/ FAIL（运行路径）。
> **不写死行号**——代码仍在改，行号会漂；用「取位置 + 比对」代替。
> 位置清单命令一律在 `D:/demo/xiuxian-danmaku` 下跑。

### 组 1 · 四色属性名（最高风险）
- **唯一源**：`Game.COLOR_CN := ["电浆","寒霜","光子","引力"]`
- **期望值**：定义处仅此一处硬编码四色中文串；其余位置**必须经 `Game.COLOR_CN[i]` 下标引用**。
- **取位置**：搜 `电浆|寒霜|光子|引力`
- **可接受的第二处硬编码**：`HelpScreen` 四色节条目「电浆 / 寒霜 / 光子 / 引力」（一句并列，属文案不可走下标）。**除此以外任何硬编码四色中文串 = 分叉 FAIL。**
- **附带核对**：`Game.COLOR_MAIN/GLOW/DARK/CORE` 四个数组**色值一个字节都不能变**（与 `06` §2.1.1 逐值比对）。

### 组 2 · 战甲五常量族（ARMOR_*）
- **唯一源**：`Game.ARMOR_TITLE / ARMOR_DESC / ARMOR_TAG / ARMOR_LORE / ARMOR_STORE`
- **期望值（终裁）**：TITLE = `["电浆剑甲","寒霜疾甲","光子盾甲","引力束甲"]`；STORE = `["电浆 · 攻","寒霜 · 速","光子 · 守","引力 · 术"]`
- **取位置**：搜 `ARMOR_TITLE|ARMOR_DESC|ARMOR_TAG|ARMOR_LORE|ARMOR_STORE`，并反向搜 `ROBE_TITLE|ROBE_DESC|ROBE_TAG|ROBE_LORE|ROBE_STORE`（**应为 0 命中**）
- **级联点**：`HUD`、`ArmorSelect`、`ArmorGallery`、`Level`（第 1 波横幅副标题）、`HelpScreen` 战甲节

### 组 3 · 品阶串（four ranks）
- **唯一源**：`Game.rank_of()`（`金勋 · 星帅` / `银勋 · 星将` / `铜勋 · 星尉` / `铁勋 · 星兵`）
- ✅ **格式已裁定（Q2）**：**以代码现状为准，`·` 两侧带空格**。team-lead 交办里的「金勋·星帅」（无空格）是笔误，**不要改成无空格**。
- 扫描宽容度：两种格式**都不要判 FAIL**（避免误伤），但**期望值记带空格**；若发现无空格版本，记 INFO 并提示「与 `rank_of()` 现状不一致，确认是否漏改」。
- **取位置**：搜 `金勋|银勋|铜勋|铁勋|星帅|星将|星尉|星兵|rank_of|品阶`
- **期望**：只有 `rank_of()` 一处定义；`ResultScreen` 经 `_rank()` 引用（不硬编码）；**不得有第二处四串**。
- **旧串残留检查**：搜 `天品|地品|玄品|黄品|元婴|金丹|筑基|炼气` → 应为 0（读码时 `Game.gd:218-220` 注释里仍有「天品可达 / 地品 ×2」，须改）

### 组 4 · Boss 名（母舰核心 / 核心）
- **唯一源**：终裁「母舰核心」（全名 星盗母舰核心，紧凑「核心」）
- **取位置**：搜 `母舰核心|始祖|老祖|血魔|巢舰`
- **期望**：`始祖` / `老祖` / `血魔` / `巢舰` **全部 0 命中**。
- **读码时的已知残留清单（执行时复核，可能已修）**：
  `Level.gd`（注:3 / 102「始祖将至」/ 111「星盗始祖 · 现身」/ 400 `_set_wave("星盗始祖")` / 413 / 418 / 423 / 449 注）、
  `HUD.gd:267`「星盗始祖 · 第 %d 阶段」、`HUD.gd:3` 注释、
  `ResultScreen.gd:50`「星盗始祖伏诛」、`HelpScreen.gd:28,57,59,65`、
  `DifficultySelect.gd`、`Boss.gd` 注释、`Game.gd:168` 注释「始祖血量」、
  **`_selftest/SelfTest.gd` 断言描述串 ≥13 处「始祖」**（`05` 未覆盖，见组 12）。

### 组 5 · 波次（波 / 星袭）+ 两个 `wave_text` 默认值 ⭐历史漏改点
- **期望**：
  - `Level.gd` 的 `wave_text` 默认值 与 `HUD.gd` 的 `wave_text` 默认值 **必须字面相同**（现均为 `"出 击"` ✅）
  - 关卡进行中：`_set_wave("第 %d 波 · 星袭")`、`"凯 旋"`、`"阵 亡"`、`"母舰核心"`
  - 第 1 波横幅主标题：`"第 一 波 · %s 星袭" % Game.COLOR_CN[t1]`
- **取位置**：搜 `wave_text`（应命中 **4 处：2 个声明 + 2 个 setter**）、搜 `星袭|第 . 波|出 击|凯 旋|阵 亡`
- **额外**：搜 `每重|三重|两重|第\s*[一二三]\s*重` → 应为 0（读码时 `Level.gd:190,199,208,209` 注释与 `HUD.gd:264` 注释「两重一条线，三重两条」仍是「重」，须改「阶段」）

### 组 6 · 单位名三层（势力 / 杂兵 / 精英 / Boss）
- **期望**：势力=**星盗**；杂兵=**星盗无人兵器**（紧凑「星盗机兵」）；精英=**星盗战将**；Boss=**母舰核心**。
- **红线**：`04` §3 —— 「星盗」是**势力层**称谓，**不得单独用来泛指杂兵**。
- **取位置**：搜 `星盗`，人工逐条判层级。期望分布：
  - 势力义（允许）：「星盗阵营」「星盗母舰」
  - 单位义（须落到具体名）：「星盗无人兵器」「星盗战将」
  - **裸「星盗」指杂兵 = CONCERNS**
- **读码时已知**：`HelpScreen.gd:28`「星盗与始祖会射出四色能量弹」→ 应改「星盗无人兵器与母舰核心会射出四色能量弹」（`08` §2.2 已给）；`HelpScreen.gd:55`「第 2、3 波末有【星盗战将】压阵」✅ 已对。

### 组 7 · 三组护盾/阶段术语（护罩 / 力场 / 阶段）
| 中文 | 对象 | 取位置 | 期望 |
| --- | --- | --- | --- |
| **护罩** | Boss 能量护罩 | 搜 `护罩` | `Level` 413 / `HUD` 278,289 / `HelpScreen` 关卡节 / `DifficultySelect` tip / `Boss.gd` 注释。**不得出现「法罩」** |
| **力场** | 精英护盾 | 搜 `力场` | `Elite` 飘字 / `Level:373` 横幅 / `HelpScreen`。**不得出现「法罡」** |
| **阶段** | Boss phase | 搜 `阶段` | `HUD:267` 第 %d 阶段 / `Level:423` / `DifficultySelect` / `HelpScreen` 计分节「破一阶段 +600」。**不得出现「法相 / 重法相」** |
- 反向：搜 `法罩|法罡|法相|重法相` → 应为 0。
- **Q3/Q4 增补两条期望值**：
  - 搜 `血光` → 应为 0；`Level.gd:418` 副标题应为「母舰核心周身泛起**赤光** · 四色螺旋弹幕」（2 字等长替换）。
  - 搜 `十息` → 应为 0；替代表述（终裁「十秒」）**必须与 `Player.SHIELD_REGEN` 的实际秒数一致**（前置依赖见 §1.2）。

### 组 8 · HP / 护盾 / 货币
- **期望**：**生命**（HUD `生命 %d / %d`、Player 注释、Result「生命耗尽」、Help 道具节、Difficulty 参数表）；**护盾**（HUD `护盾 %d`、Player）；**星币**（HUD `星币 %d`、Result、Help 计分节）。
- 反向：搜 `元神|罡气|灵石` → 应为 0。

### 组 9 · 道具四名
- **唯一源**：`Pickup.CN = ["修复包","刃影模块","增幅核心","力场罩"]`
- **取位置**：搜 `修复包|刃影|增幅|力场罩`，反向搜 `回春丹|剑影符|增攻符|无量罩`（应为 0）
- **级联**：`Player`「力场罩 · %.0f 秒」飘字、`HelpScreen` 道具节四行、`Pickup.GLYPH` 四字形。
- ⚠️ **排版红线**（`05` 待决项 2）：CN 从 3 字变 4 字（刃影模块 / 增幅核心），器形菱形字号 17，**必须在取帧里确认不溢出菱形框**（见 §4 帧 b）。

### 组 10 · 外观切换措辞（道袍 → 战甲）
- **取位置**：搜 `道袍`（应为 0）、搜 `战甲`
- **级联**：`TitleScreen` SUBS/底部提示、`HelpScreen` 战甲节与操作节「在两件战甲间互换」、`ArmorSelect` 标题「择 二 战 甲」+ 副标题 + 底部提示、`HUD.gd:247`「【空格】更换战甲」、`ResultScreen.gd:77`「T  换 甲 再 来」。

### 组 11 · 标题与品类
- **取位置**：`project.godot` 的 `config/name` / `config/description`；`TitleScreen` 标题串。
- **期望**：`config/name="星际弹幕"`；标题「星 际 弹 幕」（**字间空格风格保留、字数不变**）。
- **确认不动**：`export_presets.cfg`、三个 `.bat` 内路径、目录名 `xiuxian-danmaku`。

### 组 12 · `_selftest` 断言描述串（`05` / `08` 均未覆盖，新增）
- **事实**：`SelfTest.gd` 的 `_ck(cond, "描述")` 里**有大量中文描述串**，会出现在自测控制台输出里，属「可见文本」。
- **读码时已知命中**：`始祖` ×13（`973/974/978/1005/1006/1026/1185注/1192/1199/1200/1201注/1224`）、`第 N 重` ×5（`888/889/890/891/892`）。
- **期望**：全部按组 4 / 组 5 同步改（`始祖`→`母舰核心`/`核心`，`第 N 重`→`第 N 波`）。
- **判定**：CONCERNS（不影响 `fails` 计数，但会污染控制台输出与「0 残留」结论）。

### 组 13 · 存档键 vs 内存变量（成对陷阱）
- **事实**：内存变量已改 `picked_armors` / `armors` / `armor_idx` / `player_armors` / 信号 `armor_changed`；但**写盘 JSON 键仍是 `"robes"`**（`Game.gd:268`，`03` B 裁定不动）。
- **期望**：两边同时正确——内存侧搜 `picked_robes|player_robes|robe_idx|robe_changed` 应为 **0**；写盘侧 `"robes"` 键**必须保持**（搜 `robes` 应恰好命中 `Game.gd:122` 注释 + `Game.gd:268` 代码处）。
- **回归**：旧 `highscores.json` 里的旧品阶串（`天品·元婴` 等）只写不读（`ResultScreen` 显示实时 `_rank()`，历史最高只显示分数）→ 按 `03` D 方案 1，**无需迁移**。
- **验证动作**：人工确认 `ResultScreen` / `ArmorGallery` / 任何界面**不读 `highscores[key]["rank"]` 做展示**（`03` D 明确要求 quality-lead 复核此条）。

---

## 3 · 静默过时用例排查清单（「永远 PASS 却不测任何东西」）

| # | 用例 / 断言 | 现状（读码实测） | 为何静默 | 结论 |
| --- | --- | --- | --- | --- |
| **S1** | `tools/_check_robe.py` | 硬编码 `SKIN=(255,222,184)`、`HAIR=(26,26,38)`，对 `assets/sprites/robe_red.png` 取「脸/肤 F」「发色 K」包围盒 | ① `robe_red.png` 是死资产（未注册、运行时不加载）；② 玩家已改**纯矢量**，PNG 上根本没有玩家；③ 脚本**只 `print` 不 `SystemExit`**，bbox 为 `None` 也照样退出 0 | **删除，或改判据为矢量侧**。它测的对象已不存在，却永远退出 0。<br>⚠️ **不改 `build_robes.py:98` 的 `SKIN`**——team-lead 裁定：该脚本产出的是死资产 PNG，本次不跑不改，保留原样。**要改的是 `Player._draw()` / `ArmorArt.draw()` 的矢量绘制色。** |
| **S2** ⭐ | `07` §2.1 的 PNG 断言 **A / D / E / F / G / H**（朝向 / 亮色占比 / 尺寸 / 底部行 / 弹幕满幅 / 舱盖色） | 全部针对 `robe_*.png`；`玩家 CANOPY ≥40`、`朝向重心 25.04`、`底部行 ==35`、`44×36` 这些**基线全部诞生于 PNG 时代** | 玩家是 `Player._draw()` 矢量 + `ArmorArt` 矢量两套绘制；PNG 改了颜色**不会出现在游戏里**；断言却永远绿 | **改判据**：把这些断言的活体目标从「`robe_*.png` 像素」换成「`Player.gd` / `ArmorArt.gd` 源码静态检查」（见 S2-b）。**不得照 07 原样实现** |
| **S2-b** | 建议替代（新增 P 系列矢量静态检查） | — | — | **P1**：`Player.gd` 与 `ArmorArt.gd` 必须出现 `CANOPY (0.62,0.86,0.98)`（三通道同时匹配、容差 ≤2）；**P2**：`YokaiArt.gd`（→`PirateArt`）与 `Boss.gd` **禁止**出现该色（= `07` V1，扩到 `Boss.gd`）；**P3**：`Player.gd` / `ArmorArt.gd` / `Boss.gd` 的 CANOPY 只出现在**玩家侧** |
| **S3** | `tools/build_enemies.py` 的 `MIN_MAIN_GLOW_RATIO ≥ 30%` | 守 `enemy_*` / `elite_*`（42×42 / 84×84）——两张都是**死资产**，杂兵/精英已改 `YokaiArt.draw_minion/draw_elite` 矢量 | 脚本仍会读 PNG 算出 41.2% 并 PASS，但**游戏里的杂兵根本不是这张图** | **降级为留档自检，不计入门禁**；或直接停跑。若要活体覆盖，走 `07` §2.4 的 V0 |
| **S4** | 「按 key 取到纹理」类断言 | `ArtAssets.gd:14-15` 明载「表里没有 / 文件缺失的 key 一律返回 null，**不 push_warning**（避免污染 0-warning 门禁）」 | 任何 key 拼错 / 资产被撤（如 Boss 撤 `boss.png`）都会**静默返回 null**，既不报错也不告警 | 新增断言必须**显式断言在册 key 的 `tex(key) != null`**，并维护一份「在册 key 白名单」。Boss 撤图后「`boss` 是否仍在册」必须显式确认（撤图则应把 `"boss"` 从 `FILES` 移除，否则留一个永远 null 的死键） |
| **S5** | `SelfTest` 中依赖随机分支的 `_ck()` | `_ck()` 分支依赖随机，`PASS` 条数会漂移（近期 ≈134） | 若把「PASS 条数 == N」当门禁，会**随机红**且红得莫名其妙 | **不卡条数**（team-lead 已定）。只记录条数用于观察漂移；门禁只看 `fails / ERROR / WARNING` |
| **S6** | 赋值校验失败型缺陷 | Godot 在赋值校验失败时**跳过赋值**而非落入坏值（如「已释放对象赋给弹幕 target」） | 这类缺陷的断言**照样 PASS**，只有日志里的 ERROR 会暴露 | 门禁**必须三项同时查**：`fails==0` **且** `ERROR==0` **且** `WARNING==0`。只看任一项都会漏 |
| **S7** | `Danmaku.ART_BASE_R = 17.0` 与四张异形 PNG 尺寸的耦合 | 四张是 34×34 / 34×23 / 34×34 / 33×34，`_art.scale = r / 17` | 若换图未同步常量，视觉半径漂移、擦弹余量与碰撞脱钩；但「尺寸断言 E」在**旧图**上仍 PASS | 补「尺寸 + 常量」**联动断言**：`ART_BASE_R` 必须等于四张纹理半宽的众数；四张尺寸必须与常量表逐项对 |
| **S8** | `_capture/*.gd` 取帧脚本引用旧标识符 | `CaptureEnemy.gd` 已用 `el.player_armors` | 将来若改标识符漏改 `_capture/`，脚本直接报错 | 属**会响的用例**（显式失败，非静默），可接受。但执行取帧前**必须先跑一次确认脚本能起**（否则白等） |

---

## 4 · 实机取帧核对清单

### 4.1 前置：取帧脚手架规格

✅ **已授权（Q5）**：team-lead 批准我**在执行阶段新建取帧场景**。约束（原文照录并遵守）：

- 只新增测试脚手架，**不碰任何玩法 / 实体代码**；
- 放在 `_capture/` 下，命名清晰；
- **产物写到 `D:/demo/_qa/`**（项目外），不污染 `assets/`；
- 建完回报路径与用法。
- `--headless` 是 dummy 驱动、拿不到视口纹理 → **必须开真窗口**（不变）。

待建清单与规格：

| 场景 | 用途 | 产物 |
| --- | --- | --- |
| `_capture/Capture.gd` + `.tscn` | 主帧 a/b/c（真关卡 1.5 / 5.5 / 9.5 s） | `D:/demo/_qa/_frame_{a,b,c}.png` |
| `_capture/CapturePair.gd` + `.tscn` | 补充帧 D：玩家 vs 精英机同屏 200/300/400 px | `D:/demo/_qa/_frame_d_{200,300,400}.png` |
| `_capture/CaptureBoss.gd` + `.tscn` | 补充帧 E：直接实例化 Boss（关卡跑到 Boss 需 ~40s 且玩家会先死） | `D:/demo/_qa/_frame_e.png` |
| （复用）`_capture/CaptureUI.tscn` | 补充帧 F：择甲 / 战甲库（需扩一屏 HelpScreen + 结算） | `D:/demo/_qa/_frame_f_*.png` |

`Capture.gd` 骨架：
```
_ready():
  Game.picked_armors = [Game.RED, Game.WHITE]      # 电浆 + 光子，覆盖对称双炮管形态
  Game.difficulty = Game.NORMAL
  add_child(Level.new())
  await timer(1.5)  -> save D:/demo/_qa/_frame_a.png
  await timer(4.0)  -> save D:/demo/_qa/_frame_b.png   # 累计 5.5s
  await timer(4.0)  -> save D:/demo/_qa/_frame_c.png   # 累计 9.5s
  get_tree().quit()
```
每张前 `await RenderingServer.frame_post_draw`（沿用现有 `_capture/` 的写法）。
运行：**必须真窗口**，不得 `--headless`：
```
Godot_console.exe --path D:/demo/xiuxian-danmaku res://_capture/Capture.tscn
```

**补充帧**（三条残余风险的 §6.1 / §6.3 / §6.6 都由它们承载，缺一不可）：

| 帧 | 场景 | 内容 |
| --- | --- | --- |
| **D** | 新建 `CapturePair.tscn` | 玩家 + 精英同屏，**水平间距分别 200 / 300 / 400 px**，深空底，无弹幕干扰 → 三张或一张三联图 |
| **E** | 新建 `CaptureBoss.tscn` | 直接 `add_child(Boss.new())`（关卡跑到 Boss 需 ~40s，且玩家会先死），等入场完成后取帧 |
| **F** | **复用现有** `CaptureUI.tscn` | 择甲界面 + 战甲库（已有）；**补一帧 HelpScreen + 结算界面**（现有脚本未覆盖，需扩） |

> ✅ 帧 D / E / F 与 `Capture` 均已获授权创建（Q5）。仍**不在本轮建**——等 team-lead 下达执行指令后一次性建好并回报路径。

### 4.2 通用检查点（每张帧都要过）

- [ ] 分辨率 **1280×720**（与 `project.godot` 一致）
- [ ] **非黑屏**、非空白；玩家 / 敌人 / HUD 至少其一可见
- [ ] 控制台无 `ERROR`、无 `WARNING`（与门禁同标准）
- [ ] 无「缺字体豆腐块」、无文字溢出画布边缘

### 4.3 帧 a（1.5s）—— 第 1 波横幅

时序依据：`Level._run()` = 横幅 1.2s + `wait(0.9)` 后才出第一波 → 1.5s 处于**横幅显示中**。

- [ ] 横幅主标题 = `第 一 波 · 〈四色名〉星袭`（四色名须是 `Game.COLOR_CN[t1]`，`t1 = player.armors[1]`）
- [ ] 横幅副标题 = `换上【〈ARMOR_TITLE[t1]〉】—— 免疫同色弹幕，光刃伤害 +50%`
- [ ] HUD 右上波次文字 = **`出 击`**（`Level` 与 `HUD` 两个默认值一致）⭐
- [ ] HUD 标签：生命 / 护盾 / 星币 / 分数（不得有 元神 / 罡气 / 灵石）
- [ ] 玩家机甲可见且**朝右**；背景是星域/星云（不是山峦）；无生物化元素
- [ ] 画面中**无**「妖 / 老祖 / 道袍」等可见形态

### 4.4 帧 b（5.5s）—— 交火中（色 vs 形双通道 + 排版）

- [ ] 四色弹幕**四形可辨**（团 / 锥 / 环 / 牌），颜色 → 形状绑定正确
- [ ] **电浆（红）弹没有被星云/星野吃掉**（`07` §1.7.2：星云只用紫/品红/青、Y ≤ 0.15；红弹 Y=0.24 是四档最低）⭐
- [ ] 星野中 **≥3px 的亮点为 0**；亮星 r ≤ 1.8、alpha ≤ 0.42（不与光子白弹抢「近白亮点」）
- [ ] 玩家**舱盖 CANOPY 冷青玻璃可见**（若 §0.3 的 CANOPY 尚未落地 → 此项**直接 FAIL**，主通道缺位）
- [ ] 玩家与杂兵**余光可辨**（至少：朝向相反 + 玩家有舱盖色）
- [ ] HUD 双战甲槽显示两件战甲的 `ARMOR_TITLE` / `ARMOR_TAG`
- [ ] 道具（若出）：GLYPH 字形**不溢出菱形框**（`05` 待决项 2：CN 从 3 字变 4 字）⭐
- [ ] 飘字文本（击杀 / 力场 / 护罩）无修仙残留

### 4.5 帧 c（9.5s）—— 中后段（战将 / 波次推进）

- [ ] HUD 波次文字 = `第 N 波 · 星袭`（N=2 或 3，**不得出现「重」**）⭐
- [ ] 星盗战将（精英）若已登场：力场弧 + 身份条（`Game.COLOR_CN[color]`）文案正确
- [ ] 精英机**无 CANOPY**（光学窗走 `COLOR_CORE[c]`，不是冷青玻璃）
- [ ] 背景三层视差正常（无烘焙纹理撕裂 / 无周期接缝）→ 复核 `Background._make_nebula()` 的 `NEB_PERIOD=2048` 平铺

### 4.6 帧 D —— ⭐ 美术残余风险 ① 主通道物理尺寸 + ② 炮管互读

> 这两条是 `07` §6.1 / §6.3 点名的「断言守不住、只能靠实机」的真实缺口。

**风险 ① 主通道尺寸（🔴 高）**
- [ ] 玩家与精英机同屏、**相距 200 px**：不看颜色说明，能否**立刻**指出哪个是自己？
- [ ] 相距 **300 px**：同上
- [ ] 相距 **400 px**：同上
- [ ] 屏幕快速滚动（背景视差开启）时重复一次
- **判定**：三档**全部**「立刻可辨」= 通过；任一档犹豫 >0.5s = CONCERNS；两档以上失守 = FAIL。
- **失守时的缓解（美术已备三档，按成本从低到高）**：
  1. 舱盖面积 ~50px → ~80px（`FACE` 从 `(25,11,3,4)` 扩到 `(25,11,4,6)`，断言阈值同步提到 ≥70）
  2. 舱盖内加 1px `CORE` 高光条读作「座舱内照明」
  3. 精英机 `CORE` 光学窗加大（**注意**：会稀释朝向重心，需重算 `07` §2.1 断言 A / V2）

**风险 ② 双炮管 vs 主炮互读（🟡 中）**
- [ ] **电浆型玩家**（上下对称双炮管）与**精英机**（单根下挂主炮）同屏
- [ ] 能否靠「**对称 vs 非对称**」在 **0.5 秒内**分开？
- **判定**：0.5s 内分开 = 通过；需 1s 以上 = CONCERNS；分不开 = FAIL。
- **失守时**：精英机主炮改**上挂**或**双管不对称**——**不要维持「都是身前炮」的相似构型**。

> ⚠️ 注意：这两项**必须在 CANOPY 落地后（§0.3）才测得出**。若 CANOPY 未落地，帧 D 直接判 **FAIL（阻塞）**，不要拿「没舱盖也能分辨」当通过——那等于主通道不存在。

### 4.7 帧 E —— ⭐ 美术残余风险 ③ Boss 画面 = 母舰核心

- [ ] 画面上是**母舰核心（机械/舰船核心舱）**，**不是人形老祖**（袍服 / 人面 / 双角）⭐**此项为硬性 FAIL 门槛**
- [ ] Boss 全部绘制路径**无 CANOPY**（`07` §1.8 硬红线；`06` §4.3 敌方一致）
- [ ] 环绕「法珠」已改为**反应堆节点 / 能量球**语义形（保留 17/11 尺寸差——它是护罩色的玩法读数）
- [ ] 若 `boss.png` 已撤：`ArtAssets.FILES` 中 `"boss"` 键应**一并移除**（见 S4），否则留死键
- [ ] 若因故延期：`_art.visible = false` 至少已生效（画面损失 < 语义矛盾），且发布说明有记录

### 4.8 帧 F —— UI 层（择甲 / 战甲库 / 帮助 / 结算）

- [ ] **择甲界面**立绘 vs **关卡内玩家**视觉一致（见 §5 R1）⭐
- [ ] 战甲库 6x 立绘不糊、整数倍缩放（`ArmorArt` 无连续缩放）
- [ ] **HelpScreen**：两列末行**不压到底部提示**（`H-30 = 690`）⭐（见 §5 R2 的算式和当前越界结论）
- [ ] HelpScreen 七节文案无「始祖 / 三重 / 血光 / 十息」等残留（Q3 改「赤光」、Q4 改「十秒」）
- [ ] 结算：品阶串 = `金勋 · 星帅` 四档之一（`ResultScreen` 经 `_rank()`）；标题「母舰核心击毁 · 此役已毕」

---

## 5 · 回归风险清单（自动测试不易发现）

| # | 风险 | 具体核对方式 | 严重度 |
| --- | --- | --- | --- |
| **R1** ⭐ | **`ArmorArt`（择甲 / 战甲库 矢量立绘）与 `Player._draw()`（关卡本体）视觉不一致 → 穿帮** | 两者几何实测已**有偏差**：`ArmorArt.BODY` 与 `Player` body 六边形**完全相同** ✅，但**头部圆半径 6.2 / 3.7 vs Player 6.0 / 3.6**、`ArmorArt` 额外画了**腰带横线**而 `Player` 没有、`ArmorArt` 红色固定双刃 `oy=±17` 而 `Player` 按 `rows()` 动态（3 排时 `oy=-34/0/+34`）。**工程侧若改 `Player` 的舱盖/头部而漏改 `ArmorArt`，择甲界面与关卡里会长得不一样。** 核对：帧 F 与帧 b 同色并排比对 | **Major** |
| **R2** ⭐ | **HelpScreen 纯 `_draw()` 手绘无自动布局，文案变长会压到底部提示** | 用现成公式算：`DrawUtil.txt` 的 `pos.y` 是**基线顶部**（`draw_string` 语义），末行占用到 `y+15`。右列起点 `156`，逐节推进 `42 + n_i*21 + 21`。按读码时行数（n = 3, 6, 1, 5）算得**右列末行基线 y = 681，底部提示在 y=690** → **已越界约 6~12px**（末行与提示字重叠）。**当前就已贴线**，`08` §3 称「不会压到底部提示」是**按等长替换估的**，实际行数若 +1 立刻可见碰撞。核对：帧 F 目视 + 用公式对改动后的实际 `n_i` 复算 | **Major** |
| **R3** | 存档品阶串只写不读，旧存档读到旧串 | `ResultScreen` 显示实时 `_rank()`；历史最高只显示**分数**。需人工确认**无任何界面读 `highscores[key]["rank"]` 做展示**（`03` D 明确要 quality-lead 复核）。若无 → 旧串无害，无需迁移 | Minor |
| **R4** | Boss 撤 `boss.png` 的级联 | 撤图后 `ArtAssets.tex("boss")` 返回 null 且**不告警**（S4）。核对：`Boss._draw()` 是否有 null 兜底、是否出现「Boss 变空白」、`FILES` 是否留死键；`radius=56` 与视觉 r≤75 的 1.34 擦弹余量是否保持 | **Critical** |
| **R5** | 弹幕 `ART_BASE_R` 与异形 PNG 脱钩 | 见 S7。核对：`Danmaku.ART_BASE_R == 17` 且四张纹理尺寸 34×34 / 34×23 / 34×34 / 33×34 未变；**异形弹 `spin` 仍归零**（旋转会露馅） | Major |
| **R6** | 波次 / HUD 两处 `wave_text` 默认值分叉 | 组 5。历史漏改高发点。核对：搜 `wave_text` 的 4 处，两个默认值必须字面相同 | Major |
| **R7** | 四色名在别处硬编码，与 `COLOR_CN` 分叉 | 组 1。核对：除定义处与 `HelpScreen` 那一句并列外，不得有第三处四色中文串 | Major |
| **R8** | 背景星云抢弹幕 | 帧 b。核对：星云 Y ≤ 0.15、不用四属性色相带；星野 ≥3px 亮点为 0；红弹（Y=0.24）在深空底上仍可辨 | Major |
| **R9** | 道具 GLYPH / CN 变长溢出 | 帧 b。CN 3→4 字、字号 17 的菱形框 | Minor |
| **R10** | `_capture/*.gd` 与产品代码标识符脱节 | 组 12 的镜像。改标识符漏改 `_capture/` → 取帧直接报错（会响，但会浪费一轮） | Minor |
| **R11** | 注释/代码不一致的既有小错 | 读码时发现：`ArmorArt.gd:49` 注释「绕身引力符（**3** 枚）」但循环是 `for i in 4`（**4 枚**）；`ArtAssets.gd:13` 注释「弹幕按半径缩放（r /**9**）」但 `ART_BASE_R` 已是 **17**。✅ **已批准修正**，RT-04 一并处理 | Trivial |

---

## 6 · 判定标准（质量门）

> 门禁是**建议性（advisory）**：我给判定，最终放行由用户决定。

### PASS —— 全部满足
1. **门禁**：`运行自测.bat` → `fails=0` **且** `ERROR=0` **且** `WARNING=0`（**不卡 PASS 条数**，只记录用于漂移观察）。
2. **扫描**：A 类硬黑名单在 §1.1 范围内 **0 命中**（白名单豁免项须逐条登记在报告里）；C 类标识符残留 **0 命中**（除 JSON 键 `"robes"`）。
3. **成对表**：组 1~13 **无分叉**（注释级分叉须清零才可 PASS）。
4. **静默过时用例**：S1~S8 已按 §3 结论处置；**无新增假绿**（特别是 S2 不得按 `07` 原样实现）。
5. **取帧**：帧 a / b / c + D / E / F 全部取到（非黑屏），§4 各帧「必修项」全勾。
6. **三条美术残余风险**：③ Boss = 母舰核心 **必须为通过**；① 主通道三档距离 **全通过**；② 0.5s 内可分开。
7. **回归**：R1（ArmorArt vs Player 一致）、R2（HelpScreen 末行不压提示）、R4（Boss 撤图无空白）**均通过**。

### CONCERNS —— 满足 PASS 的 1/4/5，但有以下任一
- A 类命中**仅存在于注释**（Major，须清零）；
- B 类灰区命中未澄清（`息` / `剑` / `身法` / `此道` 等的上下文判定）；
- 成对表出现 **≤2 处**纯注释级分叉；
- 组 12（`SelfTest` 描述串）未同步（不影响 `fails`，但污染输出；Q7 已下发工程侧，改后复核）；
- 品阶串出现**无空格**版本（Q2：不判 FAIL，记 INFO）；
- 文档层（`README.md` 等）仍有残留（Q6：**不判 FAIL**，单列清单交用户）；
- 取帧 ①②③ 中任一条为「勉强 / 需 1s 以上」；
- 有静默过时用例**尚未处置**但已被识别、且不影响门禁结论。

### FAIL —— 任一满足
- 门禁 `fails > 0` **或** `ERROR > 0` **或** `WARNING > 0`；
- A 类命中出现在**玩家可见字符串**（UI 文案 / 横幅 / 道具名 / `project.godot`）；
- C 类标识符残留（内存侧 `picked_robes` / `player_robes` / `robe_idx` / `robe_changed` / `Robe*` / `YokaiArt` 任一命中）；
- 成对表出现**影响运行**的分叉（如两个 `wave_text` 默认值不一致、四色名硬编码分叉、写盘键 `"robes"` 被改）；
- **帧 E：Boss 画面仍是人形老祖**（`07` §6.6）；
- **帧 D：主通道 CANOPY 未落地**（§0.3）或三档距离两档以上失守；
- 存在**被当作绿守门的静默过时用例**（即 S2 按 `07` 原样实现、或 `_check_robe.py` 仍在跑并声称通过）；
- R4 出现「Boss 变空白 / 死键未清」。

---

## 7 · 执行顺序与工时预估

| 步 | 内容 | 预估 |
| 0 | **取 `git status` / `git log -1` 快照**（§0.1-a 纪律）+ 确认 CANOPY 已落地、Boss 撤图已完成 | 5 min |
| 0.5 | 新建取帧脚手架 `Capture` / `CapturePair` / `CaptureBoss`（Q5 已授权） | 30~40 min |
| 1 | 术语扫描（Python 扫描器 + 白名单分类 + 人工复核灰区） | 25~35 min |
| 2 | 成对表 13 组逐组取位置 + 比对 | 40~60 min |
| 3 | 静默过时用例排查（S1~S8 现状确认 + 结论落地） | 20 min（不含改码） |
| 4 | 跑门禁 `运行自测.bat`（每轮 1~2 min，通常 1~3 轮） | 10 min |
| 5 | 取帧：主帧 1 次 + 补充帧 3 次（每次约 20~30 s 含引擎启动） | 10 min |
| 6 | 6~9 张帧逐项人工核对（含 §6.1/§6.3 的同屏主观判读） | 30~45 min |
| 7 | 报告撰写（含 Bug 分级、豁免登记、判定） | 20 min |
| | **合计（含建脚手架）** | **≈ 3.0 ~ 4.0 h** |

> 说明：步骤 1/2/4 可并行推进；步骤 5/6 依赖 §0.2（脚手架）、§0.3（CANOPY 已落地）已解，否则取帧结论无效。

---

## 8 · 裁定记录（Q1~Q7 全部已裁定）

| # | 事项 | 裁定 | 落地位置 |
| --- | --- | --- | --- |
| **Q1** | 验证根目录 | ✅ **master = `D:/demo/xiuxian-danmaku`**。（我第一版判成 dev 是**错的**——拿了一次性快照当结论；dev 仓只有 Godot 打开项目产生的 `.import` 缓存） | §0.1 / §0.1-a |
| **Q2** | 品阶串格式 | ✅ **以代码现状为准：`金勋 · 星帅`（`·` 两侧带空格）**。交办里的无空格写法是笔误，不要改。扫描时两种格式都不判 FAIL，期望值记带空格 | §2 组 3 |
| **Q3** | 「血光」 | ✅ **改「赤光」**（2 字等长，不影响排版）。理由：与 `08` §1「严守无生物化词汇」冲突。**`血光` 移入 A 类，命中即 FAIL** | §1.2 / §1.3 / §2 组 7 |
| **Q4** | 「十息」 | ✅ **必须消失**，终裁「十秒」。⚠️ 实际秒数待 engineering-lead 核对 `Player.SHIELD_REGEN`；若非 10 秒则报实际值、design-strategist 重新定措辞。**扫描只判「`十息` 归零」，不锁替代表述** | §1.2 / §2 组 7 |
| **Q5** | 取帧脚手架 | ✅ **已授权我新建**。约束：只加测试脚手架不碰玩法代码、放 `_capture/`、**产物写 `D:/demo/_qa/`**、建完回报路径与用法 | §4.1 |
| **Q6** | 文档层 | ✅ **纳入扫描但��作 FAIL 门槛**。`README.md` / `工程审查报告.md` / `playtest.html` / `docs/` / `design/` 单列 `[DOC]` 清单，最后交用户定夺 | §1.1 / §1.7 |
| **Q7** | `SelfTest` 断言描述串 | ✅ **同步改**（RT-04 一并执行）。我判 CONCERNS、不影响 `fails`，改后复核 | §2 组 12 |

### 8.1 仍开放的依赖（不阻塞我开工，但会影响判定）

| # | 事项 | 归属 | 影响 |
| --- | --- | --- | --- |
| **O1** | `Player.SHIELD_REGEN` 的**实际秒数**（是否真是 10 秒） | engineering-lead 报值 → design-strategist 定措辞 | 决定「十息」的替代表述；我核「文案与实际一致」 |
| **O2** | 用户是否要一并改文档层（`README` 约 81 处命中） | 用户 | 只影响 `[DOC]` 清单的后续处置 |
| **O3** | RT-04 完成时机 | engineering-lead / team-lead | 我等通知再执行扫描与取帧 |

---

## 附 · 本方案的写作依据

读码核对过（只读、未改）：`scripts/Game.gd`、`scripts/DrawUtil.gd`、`scripts/art/ArtAssets.gd`、
`scripts/entities/{Player,Boss,Enemy,Elite,Pickup}.gd`（节选）、`scripts/ui/{ArmorArt,HelpScreen,HUD,ResultScreen}.gd`（节选）、
`scripts/world/Level.gd`（节选）、`_capture/*.gd|tscn`、`_selftest/SelfTest.gd`（节选）、
`tools/_check_robe.py`、`project.godot`、`运行自测.bat`、`git worktree list`。
