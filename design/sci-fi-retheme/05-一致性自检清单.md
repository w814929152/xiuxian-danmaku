# 05 · 一致性自检清单（quality-lead 逐条核对）

> 成对/成组出现、**必须同时改**的概念清单。任一处漏改即题材穿帮或运行错误。
> quality-lead 按此逐条扫全项目（含注释），engineering-lead 改完可自查。

---

## 组 1 · 四色属性名（最高风险，跨 6+ 处）

四色名「电浆/寒霜/光子/引力」**必须同时出现在以下所有位置**，且顺序严格对应索引 0/1/2/3：

- [ ] `Game.gd:25` `COLOR_CN := ["电浆","寒霜","光子","引力"]`（**唯一数据源**）
- [ ] `Game.gd` `COLOR_MAIN/GLOW/DARK/CORE` 四组色值**不动**（只改 COLOR_CN 内容）
- [ ] `HelpScreen.gd` 四色属性节条目「电浆 / 寒霜 / 光子 / 引力」
- [ ] `HUD.gd:278` 护罩提示 `Game.COLOR_CN[boss.ward]`（经数组引用，改数组即联动）
- [ ] `Level.gd:83` 第一波横幅 `Game.COLOR_CN[t1]`
- [ ] `Level.gd:373` 战将力场横幅 `Game.COLOR_CN[e.color]`
- [ ] `Level.gd:430` 护罩飘字 `Game.COLOR_CN[c]`
- [ ] `Elite.gd:291` 战将身份条 `Game.COLOR_CN[color]`
- [ ] `RobeSelect.gd:125` / `RobeGallery.gd:122` `免疫【%s】弹`（经 COLOR_CN 引用）
- [ ] `Enemy.gd:4` 注释四色名 / `Danmaku.gd:3` 注释四色名 / `Boss.gd` 注释四色名
- [ ] `Pickup.gd` 无四色名（道具独立），确认无残留

> **核对要点**：四色名只能改 `COLOR_CN` 数组内容，其余位置都经数组下标引用，改一处即全联动。**切勿**在别处硬编码四色中文字符串，否则会与数组分叉。

---

## 组 2 · 战甲名（ROBE_* → ARMOR_* 五常量族 + 引用点）

- [ ] `Game.gd` `ARMOR_TITLE/DESC/TAG/LORE/STORE` 五常量**全部改名 + 改内容**
- [ ] `HUD.gd:240,242` `Game.ARMOR_TITLE[c]` / `Game.ARMOR_TAG[c]`
- [ ] `RobeSelect.gd:123,129` `ARMOR_TITLE[i]` / `ARMOR_DESC[i]`
- [ ] `RobeGallery.gd:97,109,127,134,149,153` `ARMOR_STORE/TITLE/DESC/LORE/TITLE/TAG`
- [ ] `Level.gd:84` 横幅 `Game.ARMOR_TITLE[t1]`
- [ ] 全局 `rg "ROBE_"` 应为空（无残留旧常量名）

---

## 组 3 · 道袍→战甲（变量 + 信号 + 类名 + 文件）

- [ ] `picked_robes→picked_armors`（Game.gd:107 + Level/RobeSelect/RobeGallery 全部引用）
- [ ] `robes→armors`（Player.gd + Level.gd + HUD.gd 全部引用）
- [ ] `robe_idx→armor_idx`（Player.gd + HUD.gd）
- [ ] `player_robes→player_armors`（Elite.gd + Boss.gd + Level.gd）
- [ ] `robe_changed→armor_changed`（Player.gd 信号定义 + emit）
- [ ] 类名 `RobeSelect→ArmorSelect`、`RobeGallery→ArmorGallery`、`RobeArt→ArmorArt`
- [ ] 文件名同步改（含 `.uid`）
- [ ] `Main.gd:35,64` 类名引用同步
- [ ] 全局 `rg "robe" -i` 除资产键 `"robe"`（**保留不动**，team-lead 裁定死 PNG 原名不动，见 03 A.4）外应为空

---

## 组 4 · Boss 名（血魔老祖 → 母舰核心，跨 Level/Boss/HUD/Result/Help/Difficulty/Title）

> **终裁见 `08-命名终裁.md`**：Boss = 母舰核心（紧凑「核心」）。以下为 08 §2.1 覆盖后的最终值。

- [ ] `TitleScreen.gd:13` SUBS「战母舰核心」
- [ ] `Level.gd:102` 「核心将至 · 四色齐至」/ `:111` 「母舰核心 · 现身」/ `:400` `_set_wave("母舰核心")`
- [ ] `Level.gd:413` 「母舰核心不展护罩」/ `:418` 「母舰核心周身泛起血光」/ `:423` 「核心切换阶段」
- [ ] `HUD.gd:267` 「母舰核心 · 第 %d 阶段」
- [ ] `ResultScreen.gd:50` 「母舰核心击毁 · 此役已毕」
- [ ] `HelpScreen.gd:57` 「母舰核心会展开【属性护罩】」/ `:59` 「核心三阶段」/ `:65` 「击破母舰核心 +5000」
- [ ] `DifficultySelect.gd:102,110,117` 「核心生命 NNNN」/ `:127` 「母舰核心不展护罩」/ `:141` 「母舰核心上分高下」
- [ ] `Boss.gd` 注释「血魔老祖」全改
- [ ] 全局 `rg "老祖|血魔|始祖|巢舰"` 应为空（「始祖」是 02 旧值，已被 08 覆盖为「母舰核心」）

---

## 组 5 · 波次「重→波」与「妖潮→星袭」（Level.gd 集中 + HUD 默认值）

- [ ] `Level.gd:83,93,102,297` 「第 N 波 · 星袭」
- [ ] `Level.gd:297` `_set_wave("第 %d 波 · 星袭")`
- [ ] `Level.gd:33,HUD.gd:33` 默认 `wave_text="出 击"`（两处**必须一致**，Level 与 HUD 各有一个默认值）
- [ ] `Level.gd:436,443` `_set_wave("凯 旋")` / `_set_wave("阵 亡")`
- [ ] 全局 `rg "妖潮|第 . 重"` 应为空

---

## 组 6 · 法罩→护罩 / 法罡→力场 / 法相→阶段（三组护盾/阶段术语）

- [ ] **护罩**（Boss 护盾）：`Level.gd:412,413` / `HUD.gd:278,289` / `Boss.gd` 注释 / `HelpScreen` 关卡节 / `DifficultySelect` tip
- [ ] **力场**（Elite 护盾）：`Elite.gd:226,236,291` 飘字 + `Level.gd:373` 横幅 + `Elite.gd` 注释
- [ ] **阶段**（Boss phase）：`HUD.gd:267` / `Level.gd:423` / `DifficultySelect` 参数表 / `HelpScreen` 关卡节 + **计分节「破一阶段 +600」**
- [ ] 全局 `rg "法罩|法罡|法相"` 应为空（含 HelpScreen 计分节，已定改「破一阶段」）

---

## 组 7 · HP/护盾/货币（元神→生命 / 罡气→护盾 / 灵石→星币）

- [ ] **生命**：`HUD.gd:188` / `Player.gd:374,377` / `ResultScreen.gd:50,71` / `HelpScreen` 道具节 / `DifficultySelect` 参数表
- [ ] **护盾**：`HUD.gd:196,199,206` / `Player.gd:162,346`（「罡气回满/罡气 -%d」全改）
- [ ] **星币**：`HUD.gd:251` / `ResultScreen.gd:57` / `HelpScreen` 计分节
- [ ] 全局 `rg "元神|罡气|灵石"` 应为空

---

## 组 8 · 道具四名（Pickup.CN/TIP/GLYPH + 各处引用）

- [ ] `Pickup.gd:22` CN「修复包/刃影模块/增幅核心/力场罩」
- [ ] `Pickup.gd:23` TIP「生命 +20/弹道 +1/攻击 +30%/无敌 6 秒」
- [ ] `Pickup.gd:24` GLYPH「修/影/增/力」
- [ ] `Player.gd:393` 「力场罩 · %.0f 秒」
- [ ] `HelpScreen` 道具节四名 + 描述
- [ ] 全局 `rg "回春丹|剑影符|增攻符|无量罩"` 应为空

---

## 组 9 · 外观切换措辞（道袍→战甲，跨 Title/Help/RobeSelect/HUD/Result）

- [ ] TitleScreen SUBS/底部提示「战甲」
- [ ] HelpScreen 战甲节 + 操作节「在两件战甲间互换」
- [ ] RobeSelect 标题「择 二 战 甲」+ 副标题 + 底部提示
- [ ] HUD.gd:247「【空格】更换战甲」
- [ ] ResultScreen.gd:77「T  换 甲 再 来」
- [ ] 全局 `rg "道袍"` 应为空

---

## 组 10 · 标题与品类（project.godot + TitleScreen + 导出名）

- [ ] `project.godot` `config/name="星际弹幕"` / `config/description`
- [ ] `TitleScreen.gd:109,111` 标题「星 际 弹 幕」
- [ ] **确认** `export_presets.cfg` 与 `.bat` 内路径/中文名**未改**（用户约束）
- [ ] 确认 `D:/demo/xiuxian-danmaku/` 目录路径**未改**

---

## 待决项（需 team-lead / quality-lead 拍板）

1. ~~HelpScreen 计分节「破一重法相」~~ → **已定**：改「破一阶段 +600」，彻底去修仙化，且与 HUD「第 %d 阶段」措辞统一（02 已同步）。
2. **道具 CN 4 字（刃影模块/增幅核心）**：比原 3 字多 1，Pickup 器形菱形字号 17，实测宽度安全；但 quality-lead 需在实机确认不溢出菱形框。
3. ~~资产键 `"robe"→"armor"` 与 `robe_*.png→armor_*.png`~~ → **作废**：team-lead 裁定死 PNG（`robe_*`/`enemy_*`/`elite_*`）保留原名不动，资产键 `"robe"` 也保留。唯一资产变更是 Boss 撤 `boss.png` 改矢量母舰核心（art-director 07 §1.8，纳入本次交付）。

---

## 扫描命令建议（quality-lead 用）

```bash
# 修仙黑名单全扫（应仅剩白名单例外）
rg "修仙|修士|元神|道袍|飞剑|符光|符箓|赤炎|玄冰|太清|戊土|小妖|妖将|妖潮|血魔老祖|老祖|法罡|法罩|法相|罡气|灵石|问道|入定|回春丹|剑影符|增攻符|无量罩|始祖|巢舰" scripts/ project.godot
# Boss 名一致性（应全部为「母舰核心 / 核心」，无「始祖」残留）
rg "母舰核心|星盗始祖|巢舰核心" scripts/
# 敌方单位层（杂兵应为「无人兵器」，精英「星盗战将」；「星盗」单独指单位属漏改）
rg "星盗" scripts/
# 旧常量名残留
rg "ROBE_|robe_idx|picked_robes|player_robes|RobeSelect|RobeGallery|RobeArt" scripts/
# 四色名一致性（应只在 COLOR_CN 定义处 + 少数经数组引用处）
rg "电浆|寒霜|光子|引力" scripts/
```
